# Ollama 环境变量优化测试结果

## 📊 测试对比

| 配置 | 平均耗时 | 轻微修改分数 | 成功率 | 说明 |
|------|---------|------------|--------|------|
| **优化前（原始）** | 7.45s | 50.0/100 | 100% | 默认配置 |
| **优化后（环境变量）** | 8.14s | 27.5/100 | 100% | OLLAMA_NUM_THREAD=8 |

## 🔍 测试结果分析

### 性能变化
- **平均响应时间**：从 7.45s 增加到 8.14s（**变慢了 9%**）
- **轻微修改分数**：从 50.0/100 降低到 27.5/100（**质量下降**）
- **成功率**：保持 100%

### 可能的原因

1. **环境变量可能未生效**
   - macOS GUI 应用（Ollama.app）可能不会读取 `launchctl setenv` 设置的环境变量
   - 需要验证进程是否真的使用了这些环境变量

2. **模型重新加载**
   - 重启后模型需要重新加载到内存，首次运行可能较慢
   - 后续运行可能会更快

3. **deepseek-r1 特性**
   - deepseek-r1 是推理模型（thinking model），多线程优化可能不明显
   - 推理过程本身可能受限于其他因素

4. **参数配置影响**
   - 当前测试脚本使用的参数（temperature=0.15, num_predict=300）可能与多线程优化不匹配

## 💡 建议

### 1. 验证环境变量是否生效
```bash
# 检查 ollama 进程的环境变量
ps eww -p $(pgrep -f "ollama serve") | grep OLLAMA
```

### 2. 尝试其他优化方法
- **API 参数优化**：已经完成的参数优化（temperature, num_predict 等）可能更有效
- **预热模型**：首次运行后，后续请求会更快
- **使用更小的量化版本**：如果可用的话

### 3. 重新测试（预热后）
```bash
# 先运行一次预热
ollama run deepseek-r1:1.5b "预热"

# 然后再测试
ruby test_injection.rb http://127.0.0.1:11434 deepseek-r1:1.5b
```

## 🎯 结论

**环境变量优化（OLLAMA_NUM_THREAD=8）在当前测试中未显示出明显的性能提升，反而略有下降。**

可能的原因：
1. macOS GUI 应用可能无法正确读取环境变量
2. deepseek-r1 模型的推理特性可能不受多线程影响
3. 重启后模型需要重新加载

**推荐方案**：
- ✅ **继续使用 API 参数优化**（temperature=0.15, num_predict=300 等）
- ✅ **预热模型**以减少首次响应延迟
- ⚠️ **环境变量优化**：可能需要进一步验证或采用其他方法

## 📝 下一步

1. 验证环境变量是否真的被 ollama 进程读取
2. 尝试预热模型后再次测试
3. 如果环境变量未生效，考虑使用其他方法（如修改 Ollama.app 的启动配置）

