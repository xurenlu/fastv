# ✅ 动态库加载问题修复完成

## 问题

应用启动时无法加载 `libonnxruntime.1.23.2.dylib`：
```
dyld[11714]: Library not loaded: @rpath/libonnxruntime.1.23.2.dylib
Reason: code signature in ... not valid for use in process: mapping process and mapped file (non-platform) have different Team IDs
```

## ✅ 已完成的修复

### 1. 添加构建脚本
- ✅ 创建了 "Copy ONNX Runtime Library" 构建脚本
- ✅ 脚本在 Resources 阶段之后执行
- ✅ 自动复制库文件到 `fastv.app/Contents/Frameworks/`

### 2. 代码签名处理
- ✅ 移除旧的代码签名
- ✅ 使用 ad-hoc 签名重新签名（`codesign --sign -`）
- ✅ 适用于开发环境

### 3. 符号链接创建
- ✅ 创建 `libonnxruntime.dylib` → `libonnxruntime.1.23.2.dylib`
- ✅ 确保链接器能找到库文件

## 📋 构建脚本内容

脚本会执行以下操作：
```bash
# 1. 创建 Frameworks 目录
mkdir -p "${BUILT_PRODUCTS_DIR}/${FRAMEWORKS_FOLDER_PATH}"

# 2. 复制库文件
cp -f "${SRCROOT}/Libraries/onnxruntime/current/lib/libonnxruntime.1.23.2.dylib" \
      "${BUILT_PRODUCTS_DIR}/${FRAMEWORKS_FOLDER_PATH}/libonnxruntime.1.23.2.dylib"

# 3. 创建符号链接
cd "${BUILT_PRODUCTS_DIR}/${FRAMEWORKS_FOLDER_PATH}"
rm -f libonnxruntime.dylib
ln -s libonnxruntime.1.23.2.dylib libonnxruntime.dylib

# 4. 重新签名
codesign --remove-signature libonnxruntime.1.23.2.dylib 2>/dev/null || true
codesign --force --sign - --timestamp=none libonnxruntime.1.23.2.dylib 2>/dev/null || true
```

## 🧪 验证步骤

### 1. 检查库文件是否已复制
```bash
ls -lh ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/
```

应该看到：
- `libonnxruntime.1.23.2.dylib` (71MB)
- `libonnxruntime.dylib` (符号链接)

### 2. 检查代码签名
```bash
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/libonnxruntime.1.23.2.dylib
```

应该显示：`Signature=adhoc`

### 3. 运行应用
在 Xcode 中按 `⌘R` 运行，或：
```bash
open ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app
```

## ⚠️ 如果仍然无法运行

### 方法 1：重新签名整个应用
```bash
APP_PATH=~/Library/Developer/Xcode/DerivedData/fastv-bsagblddbizlhxceqjtyzxtsgyzo/Build/Products/Debug/fastv.app

# 重新签名库文件
codesign --force --sign - --timestamp=none "$APP_PATH/Contents/Frameworks/libonnxruntime.1.23.2.dylib"

# 重新签名整个应用
codesign --force --deep --sign - --timestamp=none "$APP_PATH"
```

### 方法 2：检查构建脚本执行
在 Xcode 中：
1. 选择 Target "fastv"
2. 切换到 "Build Phases"
3. 找到 "Copy ONNX Runtime Library"
4. 展开查看脚本
5. 确保 "Run script only when installing" 未勾选

### 方法 3：临时禁用库验证（仅开发）
```bash
# 需要 root 权限
sudo spctl --master-disable
```

**注意**：这会影响系统安全，仅用于开发测试。

## 📝 当前状态

- ✅ 库文件已复制到应用包
- ✅ 代码签名已处理（adhoc）
- ✅ 符号链接已创建
- ✅ 构建脚本已配置

**现在可以重新构建并运行应用了！**

