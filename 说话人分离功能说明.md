# 说话人分离功能说明

## 功能概述

为"妙打"应用添加了**自动化的说话人分离功能**，可以在会议录音中自动识别和区分不同的说话人，无需用户手动配置。

## 核心特性

### ✅ 完全自动化
- **自动检测依赖**：首次使用时自动检测并安装 Python 依赖
- **自动启动服务**：需要时自动启动 Python 服务，无需手动操作
- **自动下载模型**：首次使用时自动从 HuggingFace 下载模型（约 500MB）
- **零配置**：用户无需任何手动配置即可使用

### ✅ 智能集成
- **无缝集成**：与现有会议记录流程完美集成
- **可选启用**：默认不启用，用户可在设置中开启
- **失败容错**：说话人分离失败不影响会议记录的保存

### ✅ 高性能
- **本地处理**：所有处理都在本地完成，保护隐私
- **异步处理**：不阻塞主线程，用户体验流畅
- **智能缓存**：模型加载后缓存，后续使用更快

## 技术架构

### 1. Python 服务层

**文件位置**：`SpeakerDiarization/speaker_diarization_api.py`

- 基于 FastAPI 的 HTTP 服务
- 使用 `pyannote.audio` 3.1 模型
- 自动依赖检测和安装
- 支持最小/最大说话人数量配置

**API 端点**：
- `GET /health` - 健康检查
- `POST /api/v1/diarization` - 说话人分离

### 2. Swift 服务层

**文件位置**：
- `fastv/Services/SpeakerDiarizationService.swift` - 说话人分离服务
- `fastv/Services/DiarizationServiceManager.swift` - 服务管理器

**功能**：
- 自动启动和管理 Python 服务
- 健康检查和重试机制
- 错误处理和容错

### 3. 数据模型

**文件位置**：`fastv/Models/MeetingRecord.swift`

新增字段：
- `speakerSegments: [SpeakerSegmentInfo]` - 说话人片段列表

每个片段包含：
- `start: TimeInterval` - 开始时间（秒）
- `end: TimeInterval` - 结束时间（秒）
- `speaker: String` - 说话人标识（如 "SPEAKER_00"）
- `duration: TimeInterval` - 片段时长（秒）

## 使用方法

### 1. 启用功能

1. 打开"设置"
2. 找到"会议记录"部分
3. 勾选"启用说话人分离"

### 2. 可选配置

- **最小说话人数量**：如果知道会议至少有 N 个人，可以设置最小值
- **最大说话人数量**：如果知道会议最多有 N 个人，可以设置最大值

> **提示**：不设置这些参数时，模型会自动检测说话人数量

### 3. 使用会议记录

1. 开始会议录音
2. 停止录音后，系统会自动：
   - 转写文字
   - 修正文本
   - **分离说话人**（如果启用）
   - 生成摘要（如果启用）

### 4. 查看结果

在会议记录详情页面，可以看到：
- 每个说话人的发言片段
- 说话人标识（SPEAKER_00, SPEAKER_01 等）
- 每个片段的开始和结束时间

## 工作流程

```
用户开始录音
    ↓
用户停止录音
    ↓
1. 转写文字
    ↓
2. 修正文本
    ↓
3. AI 优化（可选）
    ↓
4. 说话人分离（如果启用）
   ├─ 自动启动 Python 服务（如果未运行）
   ├─ 保存录音为 WAV 文件
   ├─ 调用说话人分离 API
   └─ 保存说话人片段信息
    ↓
5. 生成摘要（可选）
    ↓
6. 保存会议记录
```

## 首次使用

### 自动安装流程

首次使用时，系统会自动：

1. **检测 Python 环境**
   - 检查是否安装了 Python 3
   - 如果未安装，会提示用户安装

2. **安装依赖**
   - 自动安装 `fastapi`, `uvicorn`, `pyannote.audio`, `torch`, `torchaudio`
   - 使用 `pip install --user` 安装到用户目录
   - 不会影响系统 Python 环境

3. **下载模型**
   - 首次调用时自动从 HuggingFace 下载 `pyannote/speaker-diarization-3.1` 模型
   - 模型大小约 500MB，下载时间取决于网络速度
   - 下载后缓存到本地，后续使用无需重新下载

### 注意事项

- **网络要求**：首次使用需要网络连接以下载模型
- **存储空间**：模型约占用 500MB 磁盘空间
- **处理时间**：1 小时音频约需 2-5 分钟处理（取决于硬件性能）

## 性能优化

### 模型加载
- 模型在首次使用时加载，之后缓存到内存
- 服务启动后保持运行，避免重复加载

### 处理速度
- 使用 GPU 加速（如果可用）可以显著提升速度
- 对于较长的音频，处理时间会相应增加

### 资源占用
- Python 服务在后台运行，占用内存约 1-2GB
- 处理完成后可以手动停止服务以释放资源

## 故障排查

### 问题 1：服务启动失败

**可能原因**：
- Python 3 未安装
- 依赖安装失败
- 端口被占用

**解决方法**：
1. 检查是否安装了 Python 3：`python3 --version`
2. 手动安装依赖：`pip3 install --user fastapi uvicorn pyannote.audio torch torchaudio`
3. 检查端口 50001 是否被占用

### 问题 2：模型下载失败

**可能原因**：
- 网络连接问题
- HuggingFace 访问受限

**解决方法**：
1. 检查网络连接
2. 如果使用代理，确保代理配置正确
3. 手动下载模型（需要 HuggingFace 账号）

### 问题 3：说话人分离结果不准确

**可能原因**：
- 音频质量差
- 说话人数量设置不当
- 背景噪音过大

**解决方法**：
1. 确保录音质量良好
2. 如果知道说话人数量，设置最小/最大值
3. 减少背景噪音

### 问题 4：处理速度慢

**可能原因**：
- 硬件性能不足
- 音频文件过大
- 未使用 GPU 加速

**解决方法**：
1. 使用 GPU 加速（如果支持）
2. 分段处理长音频
3. 降低音频采样率（不推荐，会影响准确性）

## 技术细节

### 模型信息

- **模型名称**：pyannote/speaker-diarization-3.1
- **模型大小**：约 500MB
- **支持格式**：WAV, MP3, M4A 等常见音频格式
- **采样率要求**：自动处理，无需特定采样率

### API 格式

**请求**：
```json
POST /api/v1/diarization
Content-Type: multipart/form-data

file: <audio_file>
min_speakers: <optional>
max_speakers: <optional>
```

**响应**：
```json
{
  "success": true,
  "segments": [
    {
      "start": 0.0,
      "end": 5.2,
      "speaker": "SPEAKER_00",
      "duration": 5.2
    },
    ...
  ],
  "speaker_count": 2,
  "total_segments": 15
}
```

## 未来改进

1. **说话人命名**：允许用户为说话人设置名称（如"张三"、"李四"）
2. **说话人识别**：基于声纹识别，自动识别已知说话人
3. **实时分离**：支持实时说话人分离（在录音过程中）
4. **批量处理**：支持对历史会议记录进行批量说话人分离
5. **导出功能**：支持导出带说话人标签的文本文件

## 更新日志

### 2025-01-XX v1.0
- 初始版本发布
- 支持自动化的说话人分离
- 集成到会议记录流程
- 自动依赖检测和安装
- 自动服务启动和管理

---

**注意**：此功能需要 Python 3 环境。如果系统未安装 Python 3，请先安装：
```bash
brew install python3
```

