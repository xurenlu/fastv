# 说话人分离功能说明

## 功能概述

说话人分离功能可以在会议录音中自动识别和区分不同的说话人。该功能基于独立的 Python RESTful 服务，用户需要自行部署服务，Mac app 通过配置的服务地址调用。

## 核心特性

### ✅ 独立服务部署
- **独立部署**：Python 服务可以独立部署在本地或远程服务器
- **灵活配置**：Mac app 通过配置的服务地址调用服务
- **易于扩展**：支持部署到多台设备或服务器集群

### ✅ 智能集成
- **无缝集成**：与现有会议记录流程完美集成
- **可选启用**：默认不启用，用户可在设置中开启
- **失败容错**：说话人分离失败不影响会议记录的保存

### ✅ 高性能
- **本地或远程处理**：可以选择本地或远程服务
- **异步处理**：不阻塞主线程，用户体验流畅
- **智能缓存**：模型加载后缓存，后续使用更快

## 技术架构

### 1. Python 服务层（独立部署）

**文件位置**：`SpeakerDiarization/`

- 基于 FastAPI 的 HTTP RESTful 服务
- 使用 `pyannote.audio` 3.1 模型
- 支持本地或远程部署
- 详细的部署文档和使用说明

**主要文件**：
- `speaker_diarization_api.py` - FastAPI 服务主文件
- `requirements.txt` - Python 依赖列表
- `start.sh` - 服务启动脚本
- `README.md` - 详细部署文档

**API 端点**：
- `GET /` - 服务信息
- `GET /health` - 健康检查
- `POST /api/v1/diarization` - 说话人分离
- `GET /docs` - Swagger API 文档

### 2. Swift 客户端层

**文件位置**：
- `fastv/Services/SpeakerDiarizationService.swift` - 说话人分离服务客户端

**功能**：
- 通过 HTTP 请求调用远程服务
- 健康检查和连接测试
- 错误处理和容错

### 3. 数据模型

**文件位置**：`fastv/Models/MeetingRecord.swift`

新增字段：
- `speakerSegments: [SpeakerSegmentInfo]` - 说话人片段列表

每个片段包含：
- `start: TimeInterval` - 开始时间（秒）
- `end: TimeInterval` - 结束时间（秒）
- `speaker: String` - 说话人标识（如 "SPEAKER_00"）
- `duration: TimeInterval` - 片段时长（秒）

## 部署指南

### 1. 服务部署

详细的部署步骤请参考 `SpeakerDiarization/README.md`。

**快速开始**：

```bash
# 进入服务目录
cd SpeakerDiarization

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # macOS/Linux

# 安装依赖
pip install -r requirements.txt

# 启动服务
./start.sh
# 或
python speaker_diarization_api.py
```

服务默认监听 `http://127.0.0.1:50001`。

### 2. Mac app 配置

1. 打开"设置"
2. 找到"会议记录"部分
3. 勾选"启用说话人分离"
4. 配置服务地址（默认：`http://127.0.0.1:50001`）
5. 点击"测试"按钮验证连接

### 3. 可选配置

在 Mac app 设置中，可以配置：
- **服务地址**：说话人分离服务的 HTTP 地址
- **最小说话人数量**：如果知道会议至少有 N 个人，可以设置最小值
- **最大说话人数量**：如果知道会议最多有 N 个人，可以设置最大值

> **提示**：不设置说话人数量参数时，模型会自动检测说话人数量

## 使用方法

### 1. 部署服务

按照 `SpeakerDiarization/README.md` 的说明部署 Python 服务。

### 2. 配置 Mac app

在设置中配置服务地址并测试连接。

### 3. 使用会议记录

1. 开始会议录音
2. 停止录音后，系统会自动：
   - 转写文字
   - 修正文本
   - **分离说话人**（如果启用且服务可用）
   - 生成摘要（如果启用）

### 4. 查看结果

在会议记录详情页面，可以看到：
- 每个说话人的发言片段
- 说话人标识（SPEAKER_00, SPEAKER_01 等）
- 每个片段的开始和结束时间

## 工作流程

```
用户开始录音
    ↓
用户停止录音
    ↓
1. 转写文字
    ↓
2. 修正文本
    ↓
3. AI 优化（可选）
    ↓
4. 说话人分离（如果启用且服务可用）
   ├─ 检查服务连接
   ├─ 保存录音为 WAV 文件
   ├─ 调用说话人分离 API
   └─ 保存说话人片段信息
    ↓
5. 生成摘要（可选）
    ↓
6. 保存会议记录
```

## 首次使用

### 服务部署流程

1. **安装 Python 环境**
   - 确保安装了 Python 3.8 或更高版本
   - 检查：`python3 --version`

2. **创建虚拟环境**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```
   注意：首次安装可能需要较长时间，因为需要下载 PyTorch 等大型依赖。

4. **配置 HuggingFace Token（可选但推荐）**
   ```bash
   export HF_TOKEN="your_token_here"
   ```

5. **启动服务**
   ```bash
   ./start.sh
   ```

6. **验证服务**
   - 访问 http://127.0.0.1:50001/health 检查服务状态
   - 访问 http://127.0.0.1:50001/docs 查看 API 文档

### 首次使用时的模型下载

首次调用服务时，会自动从 HuggingFace 下载 `pyannote/speaker-diarization-3.1` 模型（约 500MB）。下载时间取决于网络速度。下载后模型会缓存到本地，后续使用无需重新下载。

### 注意事项

- **网络要求**：首次使用需要网络连接以下载模型
- **存储空间**：模型约占用 500MB 磁盘空间
- **处理时间**：1 小时音频约需 2-5 分钟处理（取决于硬件性能）
- **服务地址**：确保 Mac app 可以访问服务地址（本地或网络可达）

## 性能优化

### 模型加载
- 模型在首次使用时加载，之后缓存到内存
- 服务启动后保持运行，避免重复加载

### 处理速度
- 使用 GPU 加速（如果可用）可以显著提升速度
- 对于较长的音频，处理时间会相应增加

### 资源占用
- Python 服务占用内存约 1-2GB
- 可以根据需要停止服务以释放资源

## 故障排查

### 问题 1：服务启动失败

**可能原因**：
- Python 3 未安装或版本过低
- 依赖安装失败
- 端口被占用

**解决方法**：
1. 检查 Python 版本：`python3 --version`（需要 3.8+）
2. 检查虚拟环境是否正确激活
3. 重新安装依赖：`pip install -r requirements.txt`
4. 检查端口 50001 是否被占用：`lsof -i :50001`
5. 使用 `--port` 参数指定其他端口

### 问题 2：模型下载失败

**可能原因**：
- 网络连接问题
- HuggingFace 访问受限

**解决方法**：
1. 检查网络连接
2. 配置代理（如果需要）：
   ```bash
   export https_proxy=http://127.0.0.1:7890
   export http_proxy=http://127.0.0.1:7890
   ```
3. 设置 HuggingFace token：
   ```bash
   export HF_TOKEN="your_token_here"
   ```

### 问题 3：Mac app 无法连接服务

**可能原因**：
- 服务未启动
- 服务地址配置错误
- 网络不可达

**解决方法**：
1. 确认服务正在运行：访问 http://127.0.0.1:50001/health
2. 检查服务地址配置是否正确
3. 在设置中点击"测试"按钮验证连接
4. 如果服务在其他机器上，检查防火墙设置

### 问题 4：说话人分离结果不准确

**可能原因**：
- 音频质量差
- 说话人数量设置不当
- 背景噪音过大

**解决方法**：
1. 确保录音质量良好
2. 如果知道说话人数量，在设置中配置最小/最大值
3. 减少背景噪音

### 问题 5：处理速度慢

**可能原因**：
- 硬件性能不足
- 音频文件过大
- 未使用 GPU 加速

**解决方法**：
1. 使用 GPU 加速（如果支持）
2. 分段处理长音频
3. 升级硬件配置

## 技术细节

### 模型信息

- **模型名称**：pyannote/speaker-diarization-3.1
- **模型大小**：约 500MB
- **支持格式**：WAV, MP3, M4A 等常见音频格式
- **采样率要求**：自动处理，无需特定采样率

### API 格式

**请求**：
```
POST /api/v1/diarization
Content-Type: multipart/form-data

file: <audio_file>
min_speakers: <optional>
max_speakers: <optional>
```

**响应**：
```json
{
  "success": true,
  "segments": [
    {
      "start": 0.0,
      "end": 5.2,
      "speaker": "SPEAKER_00",
      "duration": 5.2
    },
    ...
  ],
  "speaker_count": 2,
  "total_segments": 15
}
```

## 部署选项

### 本地部署

服务运行在同一台 Mac 上，适合个人使用。

- **优点**：数据不出本地，隐私保护最好
- **缺点**：占用本地资源

### 远程部署

服务运行在其他机器或服务器上，适合团队使用或多设备共享。

- **优点**：集中管理，资源共享
- **缺点**：需要网络连接，需要考虑安全性

## 未来改进

1. **说话人命名**：允许用户为说话人设置名称（如"张三"、"李四"）
2. **说话人识别**：基于声纹识别，自动识别已知说话人
3. **实时分离**：支持实时说话人分离（在录音过程中）
4. **批量处理**：支持对历史会议记录进行批量说话人分离
5. **导出功能**：支持导出带说话人标签的文本文件

## 更新日志

### 2025-01-XX v2.0
- 重构为独立服务部署模式
- 移除自动启动功能
- 添加服务地址配置
- 添加连接测试功能
- 完善部署文档

### 2025-01-XX v1.0
- 初始版本发布
- 支持自动化的说话人分离
- 集成到会议记录流程

---

**注意**：
- 详细部署步骤请参考 `SpeakerDiarization/README.md`
- 服务需要 Python 3.8 或更高版本
- 首次使用需要下载模型（约 500MB）
