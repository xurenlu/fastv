# ✅ 动态库签名问题修复完成

## 问题

应用启动时无法加载 `libonnxruntime.1.23.2.dylib`，错误信息：
```
code signature in ... not valid for use in process: mapping process and mapped file (non-platform) have different Team IDs
```

## ✅ 已完成的修复

### 1. 添加两个构建脚本阶段

#### 阶段 1：Copy ONNX Runtime Library
- ✅ 在 Resources 阶段之后执行
- ✅ 复制库文件到 `Frameworks` 目录
- ✅ 创建符号链接
- ✅ 使用 ad-hoc 签名（临时）

#### 阶段 2：Sign ONNX Runtime Library
- ✅ 在应用签名之后执行（`runOnlyForDeploymentPostprocessing = 1`）
- ✅ 读取应用的签名身份
- ✅ 使用相同的签名重新签名库文件

### 2. 禁用 User Script Sandboxing
- ✅ 设置 `ENABLE_USER_SCRIPT_SANDBOXING = NO`
- ✅ 允许构建脚本访问文件系统

## 📋 构建流程

1. **编译源代码**
2. **链接框架**
3. **复制资源**
4. **复制 ONNX Runtime 库**（阶段 1）
5. **应用签名**（Xcode 自动）
6. **重新签名库文件**（阶段 2，使用应用签名）

## 🧪 验证

### 检查签名一致性
```bash
# 检查应用签名
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app | grep -E "TeamIdentifier|Authority"

# 检查库文件签名
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/libonnxruntime.1.23.2.dylib | grep -E "TeamIdentifier|Authority"
```

两者应该显示相同的 `TeamIdentifier` 和 `Authority`。

### 验证签名
```bash
codesign --verify --deep --strict --verbose=2 ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app
```

应该没有错误。

## 🚀 下一步

1. **重新构建项目**（⌘B）
2. **运行应用**（⌘R）
3. **测试功能**：尝试使用语音转文字功能

## ⚠️ 如果仍然无法运行

### 手动重新签名（临时方案）

如果构建脚本没有正确执行，可以手动重新签名：

```bash
APP_PATH=~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app
LIB_PATH="$APP_PATH/Contents/Frameworks/libonnxruntime.1.23.2.dylib"

# 获取应用签名
APP_SIGN=$(codesign -dvv "$APP_PATH" 2>&1 | grep "Authority=" | head -1 | sed 's/.*Authority=\([^ ]*\).*/\1/')

# 重新签名库文件
codesign --remove-signature "$LIB_PATH"
codesign --force --sign "$APP_SIGN" --timestamp=none "$LIB_PATH"

# 重新签名整个应用
codesign --force --deep --sign "$APP_SIGN" --timestamp=none "$APP_PATH"
```

### 临时禁用安全设置（仅开发环境）

```bash
# 需要 root 权限
sudo spctl --master-disable

# 测试完成后重新启用
sudo spctl --master-enable
```

**注意**：这会影响系统安全，仅用于开发测试。

## 📝 关键点

- ✅ 两个构建脚本阶段确保库文件在正确的时间签名
- ✅ 第二个脚本在应用签名之后执行，可以读取应用的签名信息
- ✅ 使用 `runOnlyForDeploymentPostprocessing = 1` 确保脚本在应用签名之后执行
- ✅ 签名脚本会自动检测并使用应用的签名身份

**现在应该可以正常运行了！**

