# 添加 Swift Package 依赖 - 图文说明

## 📍 位置说明

### 步骤 1：选择项目文件

在 Xcode 左侧的项目导航器中：

```
fastv                    ← 点击这个蓝色图标（项目文件）
├── fastv                ← 不是这个文件夹
│   ├── ContentView.swift
│   └── ...
├── fastvTests
└── fastvUITests
```

**重要**：点击最顶部的 **蓝色圆形图标**，不是文件夹！

### 步骤 2：查看右侧面板

选择项目文件后，右侧会显示项目设置。如果没有显示：

1. 确保右侧面板已打开（View → Navigators → Show Project Navigator）
2. 点击右上角的 **"Show the inspectors"** 按钮（或按 `⌥⌘1`）

### 步骤 3：找到 Package Dependencies

在右侧面板中，你会看到多个标签页：

**如果看到这些标签**：
- General
- Signing & Capabilities  
- Build Settings
- Build Phases
- **Package Dependencies** ← 点击这个！

**如果看不到 Package Dependencies**：

#### 选项 A：通过菜单添加

1. 菜单栏 → **File** → **Add Package Dependencies...**
2. 输入：`https://github.com/microsoft/onnxruntime-swift`
3. 点击 "Add Package"

#### 选项 B：检查 Target 选择

1. 在右侧面板顶部，找到 **TARGETS** 列表
2. 确保选择了 **"fastv"**（不是 Tests）
3. 然后再查看标签页

## 🔍 详细位置图

```
Xcode 窗口布局：

┌─────────────────────────────────────────────────────────┐
│ File Edit View ...                                      │
├──────────┬──────────────────────────┬───────────────────┤
│          │                          │                   │
│ 项目导航  │  编辑器区域              │  右侧面板         │
│          │                          │                   │
│ 📁 fastv │                          │  [项目设置]       │
│   📄 ... │                          │                   │
│          │                          │  TARGETS:         │
│          │                          │  ☑ fastv          │
│          │                          │                   │
│          │                          │  [标签页]          │
│          │                          │  General           │
│          │                          │  Signing          │
│          │                          │  Build Settings   │
│          │                          │  Build Phases     │
│          │                          │  Package Dep... ← 这里！
│          │                          │  Info             │
└──────────┴──────────────────────────┴───────────────────┘
```

## 🎯 最快的方法

**使用菜单**（最简单）：

1. 打开项目：`open fastv.xcodeproj`
2. 菜单栏 → **File** → **Add Package Dependencies...**
3. 输入：`https://github.com/microsoft/onnxruntime-swift`
4. 点击 "Add Package"
5. 选择版本（建议选择最新）
6. 确保 "fastv" target 被选中
7. 点击 "Add Package"

完成！

## ❓ 如果菜单中也没有 "Add Package Dependencies"

可能的原因：
1. Xcode 版本太旧（需要 Xcode 12+）
2. 项目文件损坏

**解决方案**：
- 检查 Xcode 版本：菜单栏 → About Xcode
- 如果版本 < 12.0，需要升级 Xcode
- 或者我可以帮你手动编辑项目文件添加依赖

请告诉我你的 Xcode 版本，我可以提供更具体的帮助！

