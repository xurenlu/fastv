# App Store 权限说明文档

## 概述

妙打（FastV）是一款语音转文字输入法应用，需要在 macOS 上请求以下权限以提供核心功能。

## 请求的权限

### 1. 麦克风权限 (Microphone Access)

**权限标识**: `NSMicrophoneUsageDescription`

**用途说明**:
- 录制用户的语音输入
- 将语音转换为文字
- 这是应用的核心功能，没有此权限无法使用语音输入

**用户授权方式**:
- 首次启动应用时，系统会自动弹出权限请求对话框
- 用户点击"允许"即可授权
- 如果拒绝，可以在"系统设置 > 隐私与安全性 > 麦克风"中手动授权

**App Store 审核说明**:
- 此权限仅用于语音输入功能
- 不会录制或存储任何音频数据
- 所有语音处理都在本地完成，不会上传到服务器

### 2. 辅助功能权限 (Accessibility / Apple Events)

**权限标识**: `NSAppleEventsUsageDescription`

**用途说明**:
- **全局快捷键监听**: 监听用户设置的快捷键，在任何应用中触发语音输入
- **文本插入**: 将识别后的文本自动插入到当前聚焦的输入框中
- **剪贴板访问**: 允许用户将历史记录复制到剪贴板

**用户授权方式**:
- 首次启用语音输入功能时，系统会弹出权限请求对话框
- 用户需要点击"打开系统偏好设置"
- 在"系统设置 > 隐私与安全性 > 辅助功能"中找到"妙打"并勾选

**App Store 审核说明**:
- 此权限仅用于文本输入功能，不会访问或修改用户的任何其他数据
- 不会监控用户的键盘输入
- 不会访问其他应用的内容
- 仅在用户主动按下快捷键时才会触发功能

## 权限使用场景

### 典型使用流程

1. **首次启动**: 用户打开应用，系统请求麦克风权限
2. **设置快捷键**: 用户在应用内设置全局快捷键（如 Option+V）
3. **授权辅助功能**: 系统请求辅助功能权限，用户授权
4. **开始使用**: 用户在其他应用（如文本编辑器、聊天应用）中按下快捷键
5. **语音输入**: 应用录制语音，转换为文字，自动插入到当前输入框

### 权限必要性

- **麦克风权限**: 必需，没有此权限无法录制语音
- **辅助功能权限**: 必需，没有此权限无法：
  - 监听全局快捷键（无法在其他应用中触发功能）
  - 插入文本到其他应用（无法完成文本输入功能）

## 隐私保护

- ✅ 所有语音处理都在本地完成，不会上传到服务器
- ✅ 不会存储任何音频数据
- ✅ 不会监控用户的键盘输入
- ✅ 不会访问其他应用的内容
- ✅ 历史记录仅存储在本地，不会同步到云端

## 用户控制

用户可以随时在"系统设置 > 隐私与安全性"中：
- 撤销麦克风权限
- 撤销辅助功能权限
- 查看应用的权限使用情况

## App Store 审核要点

### 权限请求时机
- 麦克风权限：应用启动时请求（符合 Apple 指南）
- 辅助功能权限：首次使用语音输入功能时请求（符合 Apple 指南）

### 权限说明清晰度
- Info.plist 中已包含详细的权限用途说明
- 应用内提供权限状态显示和引导

### 功能完整性
- 应用在未授权时提供清晰的错误提示
- 引导用户到系统设置中授权
- 权限被拒绝时，应用仍可正常打开，但语音输入功能不可用

## 技术实现

### 沙盒模式支持
- ✅ 应用已启用 App Sandbox
- ✅ 所有文件访问都在沙盒允许的目录内（Application Support）
- ✅ 网络访问仅用于下载模型文件（用户可配置下载地址）
- ✅ 剪贴板访问在沙盒模式下自动允许

### Entitlements 配置
```xml
<key>com.apple.security.device.audio-input</key>
<true/>
<key>com.apple.security.automation.apple-events</key>
<true/>
<key>com.apple.security.network.client</key>
<true/>
```

## 常见问题

### Q: 为什么需要辅助功能权限？
A: 这是 macOS 上实现全局快捷键监听和文本插入功能的唯一方式。没有此权限，应用无法在其他应用中工作。

### Q: 应用会监控我的键盘输入吗？
A: 不会。应用只监听用户设置的快捷键，不会监控其他键盘输入。

### Q: 我的语音数据会被上传吗？
A: 不会。所有语音处理都在本地完成，不会上传任何数据。

### Q: 如何撤销权限？
A: 在"系统设置 > 隐私与安全性"中找到对应的权限设置，取消勾选"妙打"即可。

