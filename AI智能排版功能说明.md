# AI智能排版功能说明

## 功能概述

在邮件正文显示界面上方的操作按钮栏中，新增了一个 **AI智能排版** 图标按钮。点击该按钮后，AI会对邮件的HTML正文内容进行美观优化，在不改变原意的前提下改善排版布局。

## 功能特点

### 1. 智能识别
- 仅对包含HTML内容的邮件显示此按钮
- 纯文本邮件不显示此功能

### 2. 双向切换
- **首次点击**：使用AI优化排版，按钮图标变为填充样式（紫色）
- **再次点击**：恢复原始排版，按钮图标变为线框样式（灰色）
- 支持在优化版本和原始版本之间自由切换

### 3. 优化内容

AI会针对以下方面进行优化：

#### HTML结构优化
- 使用语义化标签（`<h1>-<h6>`、`<p>`、`<ul>`、`<ol>`、`<blockquote>` 等）
- 合理分段，每个段落用 `<p>` 标签包裹
- 列表内容使用正确的列表标签
- 重要信息用 `<strong>` 或 `<em>` 强调

#### 冗余内容清理
- 删除多余的 `<table>` 布局标签（改用语义化标签）
- 移除内联样式（style属性），使用软件自带的美观CSS
- 删除 `&nbsp;` 等HTML实体

#### 格式美化
- 长段落合理断句
- 引用内容用 `<blockquote>`
- 代码用 `<code>` 或 `<pre><code>`
- 保留链接的 `<a>` 标签

### 4. 考虑内置样式

AI在优化时会考虑软件内置浏览器已自动添加的CSS样式：
- 系统字体：-apple-system, PingFang SC
- 基础字号：16px，行高 1.6
- 主要文字颜色：#1d1d1f
- 链接颜色：#007AFF
- 标题使用 600 字重
- 图片圆角：8px
- 代码块背景色：#f5f5f7
- 表格边框颜色：#e5e5e5

## 使用位置

在邮件详情页面的顶部操作栏中，位于以下按钮组中：

```
[回复] [全部回复] [转发]  <空白间隔>  [AI排版✨] [星标⭐] [垃圾邮件⚠️] [删除🗑️]
```

## 技术实现

### 1. 服务层（EmailAIService）
- 新增 `optimizeHTMLLayout()` 方法
- 通过AI服务分析和重构HTML结构
- 智能移除冗余标签和样式

### 2. 视图模型层（EmailViewModel）
- 状态管理：`isOptimizingLayout`（是否正在优化）
- 缓存管理：`optimizedHTMLCache`（优化后的HTML缓存）
- 标识管理：`optimizedMessageId`（当前优化的邮件ID）

### 3. 视图层（EmailView）
- 在右侧操作按钮栏添加AI排版按钮
- 使用 `sparkles.rectangle.stack` 图标（未优化）
- 使用 `sparkles.rectangle.stack.fill` 图标（已优化，紫色）
- 优化过程中显示进度指示器

### 4. 渲染层（EmailBodyWebView）
- 优先渲染优化后的HTML
- 如果没有优化版本，则渲染原始HTML
- 保持其他功能不变（图片显示控制、文本选择等）

## 用户体验

### 视觉反馈
- **未优化状态**：线框图标，灰色
- **已优化状态**：填充图标，紫色高亮
- **优化中**：显示旋转的进度指示器
- **悬停提示**：显示"AI智能排版优化"或"恢复原始排版"

### 性能优化
- 优化结果缓存，避免重复计算
- 异步处理，不阻塞UI
- 优化失败时显示错误提示

## 注意事项

1. **内容保真**：AI优化保证不改变原文意思和信息
2. **网络依赖**：需要AI服务可用
3. **HTML专属**：仅对HTML格式邮件有效
4. **可逆操作**：随时可以恢复原始排版
5. **会话缓存**：优化结果在当前会话中保持，切换邮件后独立管理

## 应用场景

### 适合优化的邮件
- 营销邮件（复杂的表格布局）
- 富文本格式邮件（混乱的内联样式）
- 多段落长文邮件（需要更好的段落结构）
- HTML格式的通知邮件

### 不适合的邮件
- 纯文本邮件
- 已经排版良好的邮件
- 包含复杂交互的HTML邮件（如嵌入表单）

## 未来优化方向

1. **智能预判**：自动识别排版混乱的邮件并提示优化
2. **风格选择**：提供多种排版风格（简洁/商务/现代等）
3. **批量优化**：支持对多封邮件批量优化
4. **学习偏好**：记住用户的优化偏好，自动应用

---

**开发完成时间**：2025年12月2日
**版本**：v1.2.0

