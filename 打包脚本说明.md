# FastV 打包脚本说明

## 🚀 一键打包（推荐）

**完整打包流程，包含构建、签名、DMG 创建和公证：**

```bash
./package_for_distribution.sh
```

这是**最推荐的**方式，会自动完成所有步骤。

### 参数选项

```bash
# 跳过公证（仅打包和签名）
./package_for_distribution.sh --skip-notarization

# 指定所有参数（避免交互）
./package_for_distribution.sh \
  --apple-id "your-email@example.com" \
  --team-id "W49B66SUW3" \
  --app-password "your-app-specific-password"
```

## 📦 其他脚本

### 1. `build_and_sign.sh` - 构建和签名

仅构建应用并签名，不创建 DMG：

```bash
./build_and_sign.sh
```

### 2. `create_dmg.sh` - 创建 DMG

为已签名的应用创建 DMG：

```bash
./create_dmg.sh build/export/musetype.app
```

### 3. `notarize.sh` - 公证

对已签名的应用或 DMG 进行公证：

```bash
./notarize.sh build/musetype.dmg your-email@example.com TEAM_ID app-password
```

## 📋 前置要求

1. **Developer ID Application 证书**（必需）
   - 申请：https://developer.apple.com/account
   - 验证：`security find-identity -v -p codesigning | grep "Developer ID"`

2. **App-Specific Password**（用于公证，可选但推荐）
   - 创建：https://appleid.apple.com > App-Specific Passwords

## 📂 输出文件

打包完成后：
- `build/export/musetype.app` - 已签名的应用
- `build/musetype.dmg` - DMG 安装包

## 📚 详细文档

查看完整指南：`docs/DMG打包分发指南.md`

## ⚡ 快速开始

```bash
# 1. 给脚本添加执行权限
chmod +x package_for_distribution.sh

# 2. 运行打包脚本
./package_for_distribution.sh

# 3. 测试 DMG
open build/musetype.dmg
```

## 🔍 验证

```bash
# 验证应用签名
codesign -dvv build/export/musetype.app | grep Authority

# 验证 DMG 签名
codesign -dvv build/musetype.dmg | grep Authority

# 验证公证（如果已公证）
xcrun stapler validate build/musetype.dmg
```
