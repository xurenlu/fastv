# 会议软件协作功能说明

## 功能概述

实现了与会议软件的智能协作功能，包括：
1. **自动检测会议软件**：检测到会议软件运行时，立即弹出提示询问是否开始记录
2. **系统音频捕获**：支持捕获会议中对方的声音（需要 BlackHole 虚拟音频设备）

## 核心特性

### ✅ 1. 自动会议检测

- **实时监控**：每 3 秒检测一次运行中的应用
- **多软件支持**：支持腾讯会议、Zoom、Teams、钉钉、飞书等主流会议软件
- **智能提示**：检测到会议软件时立即弹出提示窗口
- **避免重复**：同一会议只提示一次

### ✅ 2. 系统音频捕获

- **捕获对方声音**：通过 BlackHole 虚拟音频设备捕获系统音频输出
- **自动检测**：自动检测 BlackHole 是否已安装
- **可选启用**：用户可以选择是否捕获系统音频
- **安装指南**：提供 BlackHole 安装指南链接

## 技术实现

### 1. 会议软件检测

**文件**：`fastv/Services/MeetingSoftwareDetector.swift`

**工作原理**：
```swift
// 每 3 秒检测一次运行中的应用
Timer.scheduledTimer(withTimeInterval: 3.0) {
    // 检查 NSWorkspace.shared.runningApplications
    // 匹配已知的会议软件 Bundle ID
}
```

**支持的会议软件**：
- 腾讯会议：`com.tencent.meeting`
- Zoom：`us.zoom.xos`
- Microsoft Teams：`com.microsoft.teams`
- 钉钉：`com.alibaba.DingTalk`
- 飞书：`com.bytedance.ee.lark`
- Google Meet：在 Chrome 中运行
- Webex：`com.cisco.webexmeetingsapp`
- Skype：`com.skype.skype`

### 2. 系统音频捕获

**文件**：`fastv/Services/SystemAudioCaptureService.swift`

**工作原理**：
1. 使用 **BlackHole** 虚拟音频设备
2. 用户在系统设置中将 BlackHole 设置为输出设备
3. 应用从 BlackHole 输入设备捕获音频

**为什么需要 BlackHole？**

macOS 本身**不支持直接捕获系统音频输出**。要捕获会议中对方的声音，需要：

1. **安装 BlackHole**：创建一个虚拟音频设备
2. **配置系统输出**：将系统音频输出到 BlackHole
3. **应用捕获**：从 BlackHole 输入设备捕获音频

## 使用流程

### 场景 1：仅记录自己的声音（默认）

```
用户启动会议软件（如腾讯会议）
    ↓
应用检测到会议软件
    ↓
弹出提示："检测到腾讯会议，是否开始记录？"
    ↓
用户点击"开始记录"
    ↓
开始录音（仅捕获麦克风输入）
    ↓
会议结束，停止录音
    ↓
转写、优化、保存
```

### 场景 2：同时记录自己和对方的声音

```
用户启动会议软件
    ↓
应用检测到会议软件
    ↓
弹出提示，用户勾选"同时捕获系统音频"
    ↓
检查 BlackHole 是否安装
    ├─ 已安装：启动系统音频捕获
    └─ 未安装：提示安装指南
    ↓
开始录音（麦克风 + 系统音频）
    ↓
会议结束，停止录音
    ↓
合并音频或分别处理
    ↓
转写、优化、保存
```

## BlackHole 安装和配置

### 1. 安装 BlackHole

1. 访问：https://github.com/ExistentialAudio/BlackHole
2. 下载最新版本（推荐 16ch 版本）
3. 安装到系统

### 2. 配置系统音频

**方法 A：使用多输出设备（推荐）**

1. 打开"系统设置 > 声音"
2. 在"输出"选项卡中，选择"多输出设备"
3. 如果没有，点击"+"添加多输出设备
4. 在多输出设备中，同时选择：
   - 你的扬声器/耳机（用于听声音）
   - BlackHole（用于应用捕获）

**方法 B：直接使用 BlackHole（不推荐）**

1. 打开"系统设置 > 声音"
2. 在"输出"选项卡中，选择"BlackHole"
3. ⚠️ **注意**：这样你就听不到声音了，需要配合其他方法

### 3. 验证安装

应用会自动检测 BlackHole 是否可用。如果检测到，会在提示窗口中显示"已检测到 BlackHole"。

## 代码集成

### 在应用启动时启用检测

```swift
// 在 fastvApp.swift 或应用启动时
MeetingSoftwareDetector.shared.startMonitoring()
```

### 处理会议检测

```swift
// 在 MeetingRecordViewModel 中
meetingDetector.setOnMeetingDetected { meeting in
    // 显示提示窗口
    showMeetingDetectionAlert = true
}
```

### 开始录音（带系统音频）

```swift
// 如果用户选择捕获系统音频
if shouldCaptureSystemAudio {
    let systemAudioURL = // 临时文件 URL
    try await systemAudioCapture.startCapture(to: systemAudioURL)
}

// 开始正常录音
startRecording()
```

## 注意事项

### 1. 权限要求

- **麦克风权限**：需要麦克风权限才能录音
- **辅助功能权限**：不需要（会议检测不需要）

### 2. 性能影响

- **检测频率**：每 3 秒检测一次，对性能影响很小
- **系统音频捕获**：会增加 CPU 和内存使用

### 3. 音频质量

- **麦克风输入**：取决于麦克风质量
- **系统音频**：取决于会议软件的音频质量
- **合并处理**：如果需要合并两个音频源，需要额外的处理

### 4. 法律合规

⚠️ **重要**：在录制会议时，务必：
- 告知所有参与者正在录音
- 获得所有参与者的同意
- 遵守当地法律法规

## 故障排查

### 问题 1：检测不到会议软件

**可能原因**：
- 会议软件的 Bundle ID 不在支持列表中
- 应用未运行

**解决方法**：
1. 检查会议软件是否在运行
2. 如果使用新的会议软件，可以添加到 `meetingSoftwares` 列表

### 问题 2：BlackHole 检测不到

**可能原因**：
- BlackHole 未安装
- 未正确配置

**解决方法**：
1. 确认 BlackHole 已安装
2. 在"系统设置 > 声音"中检查是否有 BlackHole 设备
3. 重启应用

### 问题 3：系统音频捕获失败

**可能原因**：
- BlackHole 未设置为输出设备
- 音频格式不匹配

**解决方法**：
1. 检查系统输出设备设置
2. 确保 BlackHole 在多输出设备中已启用
3. 重启应用

## 未来改进

1. **自动配置 BlackHole**：尝试自动配置系统音频输出
2. **音频合并**：自动合并麦克风和系统音频
3. **更多会议软件**：支持更多会议软件
4. **会议状态检测**：检测会议是否真正在进行（不仅仅是应用运行）
5. **自动停止**：会议结束后自动停止录音

## 更新日志

### 2025-01-XX v1.0
- 初始版本发布
- 支持自动检测会议软件
- 支持系统音频捕获（需要 BlackHole）
- 支持多款主流会议软件

---

**提示**：要完整记录会议（包括对方声音），需要安装并配置 BlackHole 虚拟音频设备。

