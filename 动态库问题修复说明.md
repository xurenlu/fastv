# 动态库加载问题修复说明

## 问题

应用启动时无法加载 `libonnxruntime.1.23.2.dylib`，错误信息：
```
dyld[11714]: Library not loaded: @rpath/libonnxruntime.1.23.2.dylib
```

## 原因

1. **动态库未复制到应用包**：库文件需要复制到 `fastv.app/Contents/Frameworks/` 目录
2. **代码签名问题**：库文件的代码签名与应用不匹配

## 解决方案

### 已完成的修复

1. ✅ **添加构建脚本**：在构建阶段自动复制库文件到 Frameworks 目录
2. ✅ **代码签名处理**：移除旧签名并使用 ad-hoc 签名重新签名
3. ✅ **创建符号链接**：创建 `libonnxruntime.dylib` 指向版本化文件

### 构建脚本功能

构建脚本 (`Copy ONNX Runtime Library`) 会：
1. 创建 `Frameworks` 目录（如果不存在）
2. 复制 `libonnxruntime.1.23.2.dylib` 到应用包
3. 创建符号链接 `libonnxruntime.dylib`
4. 移除旧的代码签名
5. 使用 ad-hoc 签名重新签名（适用于开发）

### 验证

构建成功后，检查：
```bash
ls -lh ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/
```

应该能看到：
- `libonnxruntime.1.23.2.dylib`
- `libonnxruntime.dylib` (符号链接)

## 如果仍然无法运行

### 方法 1：手动复制（临时方案）

```bash
# 找到构建的应用
APP_PATH=~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app

# 创建 Frameworks 目录
mkdir -p "$APP_PATH/Contents/Frameworks"

# 复制库文件
cp Libraries/onnxruntime/current/lib/libonnxruntime.1.23.2.dylib "$APP_PATH/Contents/Frameworks/"

# 创建符号链接
cd "$APP_PATH/Contents/Frameworks"
ln -sf libonnxruntime.1.23.2.dylib libonnxruntime.dylib

# 重新签名
codesign --remove-signature libonnxruntime.1.23.2.dylib 2>/dev/null || true
codesign --force --sign - --timestamp=none libonnxruntime.1.23.2.dylib
```

### 方法 2：检查构建脚本执行

在 Xcode 中：
1. 选择 Target "fastv"
2. 切换到 "Build Phases"
3. 找到 "Copy ONNX Runtime Library" 脚本
4. 展开查看脚本内容
5. 确保脚本在 "Resources" 阶段之后执行

### 方法 3：禁用代码签名验证（仅开发环境）

```bash
# 临时禁用库验证（需要 root）
sudo spctl --master-disable
```

**注意**：这会影响系统安全，仅用于开发测试。

## 下一步

1. 重新构建项目
2. 运行应用
3. 如果仍有问题，检查构建日志中的脚本执行输出

