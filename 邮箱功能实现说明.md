# 邮箱功能实现说明

## 当前状态

邮箱功能的完整代码框架已经实现，包括：

✅ **数据模型层**：EmailAccount、EmailMessage、EmailFolder、EmailStore
✅ **数据库层**：EmailDatabase（使用 GRDB）
✅ **服务层**：EmailService、EmailAIService、EmailNotificationService、AvatarService
✅ **视图模型**：EmailViewModel、EmailAccountViewModel
✅ **UI界面**：EmailView、EmailAccountManagementView、EmailSettingsTab
✅ **集成**：已添加到 ContentView 和 SettingsView

## 依赖安装问题

### 已移除的依赖
- ❌ `MailCore2`：在 CocoaPods 上已弃用，且网络连接有问题
- ❌ `SwiftIdenticon`：可能不存在，已使用自定义实现

### 需要安装的依赖
- ✅ `GRDB.swift`：SQLite 数据库库（必需）

### 安装步骤

```bash
# 设置代理
export https_proxy=http://127.0.0.1:7856
export http_proxy=http://127.0.0.1:7856
export all_proxy=socks5://127.0.0.1:7856

# 安装依赖（只安装 GRDB.swift）
pod install
```

如果 `pod install` 很慢，可以：
1. **先编译项目**：代码框架已完整，可以先编译查看是否有语法错误
2. **后台安装**：让 pod install 在后台运行
3. **使用 Swift Package Manager**：考虑将 GRDB.swift 改为 SPM 方式添加

## 待完成的工作

### 1. MailCore2 集成（重要）

`EmailService.swift` 中目前是框架代码，需要集成实际的邮件库。有几个选择：

**选项 A：手动集成 MailCore2**
- 从 GitHub 下载 MailCore2 源码：https://github.com/MailCore/mailcore2
- 手动编译并添加到项目
- 创建桥接头文件

**选项 B：使用其他邮件库**
- MailKit（Apple 官方，但需要 macOS 12+）
- 纯 Swift 实现 IMAP/SMTP 协议（工作量较大）

**选项 C：暂时使用模拟实现**
- 先实现基础的连接测试和邮件列表展示
- 后续再集成实际的邮件收发功能

### 2. 功能完善

- [ ] 实现实际的 IMAP/SMTP 连接（需要 MailCore2）
- [ ] 实现邮件同步逻辑
- [ ] 实现邮件发送功能
- [ ] 完善 HTML 邮件渲染（当前使用简单文本显示）
- [ ] 实现附件下载和预览

### 3. 测试

- [ ] 测试多账号管理
- [ ] 测试数据库存储和查询性能
- [ ] 测试 AI 功能（自动回复、智能标签等）
- [ ] 测试系统通知

## 当前可以使用的功能

即使没有 MailCore2，以下功能已经可以工作：

1. ✅ **账号管理界面**：可以添加、编辑、删除账号（数据会保存到数据库）
2. ✅ **数据库存储**：邮件数据会保存到 SQLite 数据库
3. ✅ **UI界面**：所有界面都已实现，可以查看和交互
4. ✅ **设置界面**：邮箱相关设置已集成到设置中
5. ✅ **头像显示**：Gravatar + Clearbit + Identicon 已实现

## 下一步建议

1. **先编译项目**：检查是否有语法错误
2. **安装 GRDB.swift**：运行 `pod install`（可能需要一些时间）
3. **测试 UI**：可以先测试界面和账号管理功能
4. **集成邮件库**：选择合适的方案集成实际的邮件收发功能

## 注意事项

- MailCore2 是 C++ 库，需要创建 Objective-C 桥接头文件
- 密码存储在 Keychain 中，确保应用有 Keychain 访问权限
- 数据库文件位于 `~/Library/Application Support/fastv/Email/email.db`
- 头像缓存位于 `~/Library/Caches/EmailAvatars`

