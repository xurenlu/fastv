# 邮件正文缓存丢失问题修复

## 问题描述

用户反馈：已经查看过的邮件（包括做过 AI 摘要的邮件），在下次启动应用时还需要重新从服务器加载正文，即使正文已经缓存到本地数据库。

## 问题根源

### 1. 数据库使用 `INSERT OR REPLACE`

在 `EmailStore.swift` 的 `saveMessage` 方法中（第 341 行），使用了 `INSERT OR REPLACE` 语句：

```sql
INSERT OR REPLACE INTO email_messages (
    id, account_id, folder_id, uid, message_id, thread_id,
    subject, from_name, from_email, to_contacts, cc_contacts,
    bcc_contacts, reply_to_contacts, text_body, html_body, preview,
    ...
) VALUES (?, ?, ?, ?, ?, ?, ...)
```

这意味着每次保存邮件时，会**完全覆盖**数据库中的现有记录。

### 2. 状态更新方法使用了不完整的邮件对象

在 `EmailViewModel.swift` 中，以下方法在更新邮件状态时，直接使用传入的 `message` 参数创建 `updated` 对象：

- `markAsRead(_:)` - 标记为已读
- `toggleStar(_:)` - 切换星标
- `deleteMessage(_:)` - 删除邮件
- `markAsSpam(_:)` - 标记为垃圾邮件
- `restoreFromSpam(_:)` - 取消垃圾邮件标记

**问题场景**：

1. 用户查看邮件 A，正文被加载并保存到数据库（包含 `textBody` 和 `htmlBody`）
2. 用户切换到邮件列表，此时列表中的邮件对象**不包含正文**（为了性能，列表只加载基本信息）
3. 用户点击邮件 A 的"标记为已读"按钮
4. `markAsRead` 方法接收到的 `message` 参数是列表中的邮件对象，**没有正文**
5. 创建 `updated` 对象时，`textBody` 和 `htmlBody` 都是 `nil`
6. `updateMessage` 将这个没有正文的邮件保存到数据库
7. `INSERT OR REPLACE` 用 `nil` 覆盖了之前保存的正文！

### 3. 日志证据

从用户提供的日志可以看到：

```
✅ [LibEtPan] 标记为已读成功，UID: 189352
✅ [EmailStore] 更新内存中的邮件: renlu, mergely.app will expire in 7 days - renew now, 
    folderId=FDFAE604-0EA4-4DF0-80B4-44429752B61A, hasBody=false, htmlBody长度=0
```

在标记为已读后，邮件被更新到数据库，但 `hasBody=false, htmlBody长度=0`，说明正文被清空了。

## 解决方案

在所有状态更新方法中，**在创建 `updated` 对象之前，先从 `emailStore` 获取最新的邮件数据**（包含正文缓存）：

```swift
// 修复前
var updated = message
updated.isRead = true
try await emailStore.updateMessage(updated)

// 修复后
var updated = message
if let folderId = message.folderId,
   let storeMessages = emailStore.messages[folderId],
   let latestMessage = storeMessages.first(where: { $0.id == message.id }) {
    updated = latestMessage  // 使用 EmailStore 中的最新数据（包含正文）
    print("✅ [EmailViewModel] markAsRead: 使用 EmailStore 中的最新邮件数据（保留正文缓存）")
} else {
    print("⚠️ [EmailViewModel] markAsRead: 未找到 EmailStore 中的邮件，使用传入的邮件对象")
}

updated.isRead = true
try await emailStore.updateMessage(updated)
```

## 修复的方法列表

已修复以下 5 个方法：

1. ✅ `markAsRead(_:)` - 标记为已读（第 968-995 行）
2. ✅ `toggleStar(_:)` - 切换星标（第 1400-1423 行）
3. ✅ `deleteMessage(_:)` - 删除邮件（第 1426-1457 行）
4. ✅ `markAsSpam(_:)` - 标记为垃圾邮件（第 1460-1478 行）
5. ✅ `restoreFromSpam(_:)` - 取消垃圾邮件标记（第 1481-1499 行）

## 验证方法

修复后，可以通过以下步骤验证：

1. 打开一封邮件，等待正文加载完成
2. 查看日志，确认正文已保存：`✅ [EmailViewModel] 邮件正文已保存到数据库和 EmailStore`
3. 标记邮件为已读（如果之前未读）
4. 查看日志，应该看到：`✅ [EmailViewModel] markAsRead: 使用 EmailStore 中的最新邮件数据（保留正文缓存）`
5. 重启应用
6. 再次打开同一封邮件
7. 查看日志，应该看到：`✅ [EmailViewModel] 使用本地正文缓存`，而不是 `📧 [EmailViewModel] 开始加载邮件正文`

## 其他说明

### 为什么 `analyzeMessageWithAI` 不需要修复？

`analyzeMessageWithAI` 方法（第 1130 行）在开始时就从 `messages` 列表中获取了最新的邮件：

```swift
let msg = messages.first(where: { $0.id == message.id }) ?? message
```

这个 `messages` 列表是 ViewModel 维护的当前显示的邮件列表，包含了已加载的正文，所以不会有问题。

### 为什么 `loadMessageBody` 中的 `updateMessage` 不需要修复？

在 `loadMessageBody` 方法的第 889 行有一个 `updateMessage` 调用，但这个调用是在检查到邮件已有正文缓存的情况下执行的（`shouldUseCache = true`），所以 `message` 对象肯定包含正文，不会导致正文丢失。

## 性能影响

这个修复对性能的影响极小：

- 只是在更新前多了一次内存查找（O(n) 复杂度，n 是文件夹中的邮件数量）
- `emailStore.messages[folderId]` 是内存中的字典查找，非常快
- `first(where:)` 在大多数情况下会很快找到邮件（因为通常是刚刚操作的邮件）

## 总结

这是一个典型的**数据一致性问题**：

- ✅ 正文缓存机制本身是正常工作的
- ✅ 数据库读写逻辑是正确的
- ❌ 问题出在状态更新时使用了不完整的数据对象
- ✅ 修复方法是确保更新前使用最新的完整数据

修复后，邮件正文缓存将正常工作，用户不需要重复加载已查看过的邮件正文。

