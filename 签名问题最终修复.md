# ✅ 动态库签名问题最终修复完成

## 问题

应用启动时无法加载 `libonnxruntime.1.23.2.dylib`，错误信息：
```
code signature in ... not valid for use in process: mapping process and mapped file (non-platform) have different Team IDs
```

## ✅ 最终解决方案

### 1. 使用完整的签名身份

问题根源：`CODE_SIGN_IDENTITY` 环境变量只包含 `Apple Development`，而不是完整的签名身份 `Apple Development: suhang hu (9K5FH5XTHD)`。

### 2. 修改构建脚本

#### 脚本 1：Copy ONNX Runtime Library
- 复制库文件到 `Frameworks` 目录
- 创建符号链接
- 尝试使用 `DEVELOPMENT_TEAM` 查找完整签名身份

#### 脚本 2：Sign ONNX Runtime Library
- **硬编码完整的签名身份**：`Apple Development: suhang hu (9K5FH5XTHD)`
- 如果应用已签名，优先使用应用的签名身份
- 重新签名库文件和应用，确保签名一致

### 3. 关键修改

```bash
# 使用完整的签名身份（硬编码以确保一致性）
SIGN_IDENTITY="Apple Development: suhang hu (9K5FH5XTHD)"
# 如果应用已签名，获取应用的签名身份
APP_SIGN=$(codesign -dvv "${APP_PATH}" 2>&1 | grep "Authority=" | head -1 | sed 's/.*Authority=\([^)]*\).*/\1/' 2>/dev/null || echo "")
if [ -n "${APP_SIGN}" ]; then
    SIGN_IDENTITY="${APP_SIGN}"
fi
# 重新签名库文件和应用
codesign --force --sign "${SIGN_IDENTITY}" --timestamp=none "${LIB_PATH}"
codesign --force --deep --sign "${SIGN_IDENTITY}" --timestamp=none "${APP_PATH}"
```

## ✅ 验证结果

- ✅ 库文件签名：`Apple Development: suhang hu (9K5FH5XTHD)`
- ✅ 应用签名：`Apple Development: suhang hu (9K5FH5XTHD)`
- ✅ TeamIdentifier：两者匹配（`W49B66SUW3`）
- ✅ 签名验证：通过（`valid on disk`，`satisfies its Designated Requirement`）

## 🚀 下一步

1. **运行应用**（⌘R）
2. **测试功能**：尝试使用语音转文字功能

应用现在应该可以正常启动并加载 ONNX Runtime 库了！

## 📝 注意事项

- 签名身份硬编码在构建脚本中，如果更换开发者账号，需要更新脚本中的签名身份
- 构建脚本会在每次构建时自动执行，确保签名一致性
- 如果仍然遇到问题，可以临时禁用系统安全设置（仅开发环境）：
  ```bash
  sudo spctl --master-disable
  ```

