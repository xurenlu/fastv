# AI 文本优化功能 - 更新总结

## 本次更新内容

### 新增功能

#### 1. 超时时间设置 ⏱️
- 添加可调节的超时时间滑块（2-30 秒）
- 默认超时时间：5 秒
- 实时显示当前超时值
- 提供使用建议（本地 2-5 秒，远程 10-30 秒）

#### 2. 测试文本优化功能 🧪
- 发送实际测试文本进行优化
- 显示优化前后的对比
- 显示实际响应时间
- 提供速度评估（非常快/正常/较慢/太慢）
- 根据响应时间给出优化建议

### 改进功能

#### 1. OllamaService 增强
- `optimizeTranscript()` 方法新增 `timeout` 参数
- 新增 `testOptimization()` 方法用于测试
- 所有 API 请求都支持自定义超时时间
- 改进日志输出，显示超时设置和实际耗时

#### 2. 设置界面优化
- 重新布局测试按钮，更加清晰
- "测试文本优化"按钮使用醒目的主题色
- 添加超时时间滑块，带实时预览
- 改进提示信息，更加详细

#### 3. 实际使用流程优化
- 语音识别完成后，AI 优化会记录耗时
- 控制台日志显示详细的性能数据
- 超时失败时自动降级使用原始文本

## 更新的文件

### 核心代码文件
1. `fastv/Models/UserPreferences.swift` - 添加 `aiTimeout` 设置项
2. `fastv/Services/OllamaService.swift` - 添加超时参数和测试方法
3. `fastv/Views/SettingsView.swift` - 添加超时滑块和测试按钮
4. `fastv/fastvApp.swift` - 集成超时参数和性能日志

### 文档文件
1. `AI文本优化功能说明.md` - 更新完整功能说明
2. `AI优化快速开始.md` - 更新快速开始指南
3. `更新总结_AI优化增强版.md` - 本文件

## 使用指南

### 推荐的配置流程

1. **启用功能**
   ```
   设置 > AI 文本优化 > 启用 AI 文本优化 ✓
   ```

2. **配置基本信息**
   ```
   API 端点: http://127.0.0.1:11434
   模型名称: gemma2:2b
   超时时间: 5.0 秒（先使用默认值）
   ```

3. **测试连接**
   ```
   点击"测试连接"按钮 → 确认连接成功
   ```

4. **测试优化效果**（重要！）
   ```
   点击"测试文本优化"按钮
   → 查看优化结果
   → 查看响应时间
   → 根据响应时间调整超时设置
   ```

5. **调整超时时间**
   ```
   如果测试响应时间是 2.5 秒
   → 建议设置超时为 3-4 秒
   
   如果测试响应时间是 8 秒
   → 建议设置超时为 10 秒，或更换更快的模型
   ```

### 超时时间设置建议

| 测试响应时间 | 建议超时设置 | 评估 |
|------------|------------|------|
| < 2 秒 | 2-3 秒 | ⚡️ 非常快，完美 |
| 2-3 秒 | 3-5 秒 | ✅ 正常，推荐 |
| 3-5 秒 | 5-7 秒 | ✅ 可接受 |
| 5-8 秒 | 8-10 秒 | ⚠️ 较慢，考虑换模型 |
| > 8 秒 | 10-15 秒 | ❌ 太慢，建议换模型 |

## 测试示例

### 测试成功的示例

```
✅ 测试成功

⏱️ 响应时间: 2.34 秒 ✅ 正常

📝 原始文本:
嗯那个我今天想去超市买点东西然后呢顺便看看有没有什么优惠活动

✨ 优化后:
我今天想去超市买点东西，顺便看看有没有什么优惠活动。

💡 提示:
• 响应时间 < 5 秒：适合实时语音输入
• 响应时间 > 5 秒：建议使用更小的模型或调整超时设置
```

### 不同模型的性能对比

基于实际测试（仅供参考，实际性能取决于硬件）：

| 模型 | 大小 | 响应时间 | 中文质量 | 推荐度 |
|------|------|---------|---------|--------|
| qwen2:1.5b | 1.5B | 1-2 秒 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| gemma2:2b | 2B | 2-3 秒 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| llama3.2:1b | 1B | 1-2 秒 | ⭐⭐⭐ | ⭐⭐⭐ |
| qwen2:7b | 7B | 5-8 秒 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

**推荐**：对于实时语音输入，优先选择 qwen2:1.5b 或 gemma2:2b

## 技术细节

### 超时处理机制

1. **请求级别超时**：每个 API 请求都有独立的超时设置
2. **优雅降级**：超时后自动使用原始转录文本
3. **不阻塞主流程**：AI 优化失败不影响语音输入功能
4. **详细日志**：记录每次优化的耗时，便于调试

### 性能监控

应用会在控制台输出详细的性能日志：

```
🤖 [fastvApp] AI 优化已启用，开始优化文本（超时: 5.0秒）...
🤖 [OllamaService] 发送请求到 Ollama（超时: 5.0秒）...
✅ [fastvApp] AI 优化完成，耗时: 2.34秒
```

### API 请求格式

```json
{
  "model": "gemma2:2b",
  "prompt": "原始转录文本",
  "system": "系统提示词",
  "stream": false,
  "format": "json",
  "options": {
    "temperature": 0.3,
    "top_p": 0.9
  }
}
```

超时时间通过 `URLRequest.timeoutInterval` 设置。

## 故障排查

### 问题：测试时提示"连接失败"

**解决方法**：
1. 确认 Ollama 服务正在运行：`ollama serve`
2. 检查端点地址是否正确
3. 尝试在终端运行：`curl http://127.0.0.1:11434/api/tags`

### 问题：测试时提示"请求失败"

**解决方法**：
1. 确认模型已下载：`ollama list`
2. 如果没有，下载模型：`ollama pull gemma2:2b`
3. 检查模型名称是否正确（区分大小写）

### 问题：经常超时

**解决方法**：
1. 使用"测试文本优化"查看实际响应时间
2. 增加超时设置（建议比测试时间多 20-30%）
3. 考虑更换更快的模型
4. 检查系统资源占用

### 问题：优化效果不理想

**解决方法**：
1. 尝试不同的模型
2. 确保使用的是针对中文优化的模型（如 qwen2）
3. 检查原始转录文本的质量

## 后续优化方向

1. **自动超时调整**：根据历史响应时间自动调整超时设置
2. **模型性能基准**：内置常见模型的性能数据
3. **批量测试**：支持测试多个模型并对比
4. **自定义测试文本**：允许用户输入自己的测试文本
5. **性能统计**：记录和显示 AI 优化的历史性能数据

## 版本信息

- **版本**：v1.1
- **更新日期**：2025-11-21
- **兼容性**：macOS 12.0+
- **依赖**：Ollama（本地）或兼容的 API 服务

## 反馈与支持

如果遇到问题或有改进建议，请：
1. 查看控制台日志获取详细错误信息
2. 使用"测试文本优化"功能诊断问题
3. 检查 Ollama 服务状态
4. 尝试不同的超时设置和模型

祝使用愉快！🎉

