# 线程优先级反转修复说明

## 问题描述

在运行应用时，Xcode 的 Thread Sanitizer 或 Instruments 报告了以下警告：

```
hang risk:/Users/rocky/Sites/fastv/ThirdParty/libetpan/src/data-types/mailstream_cfstream.c:912 
[Internal] Thread running at User-initiated quality-of-service class waiting on a lower QoS thread running at Default quality-of-service class. 
Investigate ways to avoid priority inversions
```

## 根本原因

LibEtPan 库使用 CFStream 进行网络 I/O 操作。当调用 `mailimap_ssl_connect`、`mailsmtp_ssl_connect` 等函数时，这些函数内部会：

1. 调用 `CFReadStreamOpen` 或 `CFWriteStreamOpen` 打开流
2. 调用 `wait_runloop` 函数等待 runloop 处理网络事件
3. `wait_runloop` 函数调用 `CFRunLoopRunInMode` 运行当前线程的 runloop

问题在于：
- Swift 代码使用 `Task.detached(priority: .userInitiated)` 创建任务，任务本身标记为 User Initiated QoS
- 但是 LibEtPan 的 C 代码调用 `CFRunLoopGetCurrent()` 获取当前线程的 runloop
- 如果 runloop 运行在较低 QoS（Default）的线程上，就会导致 User Initiated QoS 的线程等待 Default QoS 的线程，产生优先级反转

## 解决方案

在所有可能触发 runloop 等待的 LibEtPan 函数调用之前，使用 `pthread_set_qos_class_self_np` 将当前线程的 QoS 设置为 `QOS_CLASS_USER_INITIATED`，确保 runloop 运行在正确的 QoS 级别。

### 修改内容

1. **添加辅助函数**：在 `LibEtPanWrapper.m` 中添加了 `ensureUserInitiatedQoS()` 函数

```objective-c
#import <pthread/qos.h>

/// 设置当前线程的 QoS 为 User Initiated，避免优先级反转
static void ensureUserInitiatedQoS(void) {
    pthread_set_qos_class_self_np(QOS_CLASS_USER_INITIATED, 0);
}
```

2. **在所有网络 I/O 操作前调用 QoS 设置**：

#### IMAP 操作：
- `connectWithError:` - IMAP 连接（`mailimap_ssl_connect`, `mailimap_socket_connect`, `mailimap_socket_starttls`）
- `loginWithError:` - IMAP 登录（`mailimap_login`）
- `selectFolder:error:` - 选择文件夹（`mailimap_select`）
- `fetchFoldersWithError:` - 获取文件夹列表（`mailimap_list`）
- `fetchMessagesFromUID:toUID:error:` - 搜索邮件（`mailimap_uid_search`）
- `fetchMessagesSinceDate:limit:error:` - 按日期搜索（`mailimap_uid_search`）
- `searchMessagesWithQuery:limit:error:` - 文本搜索（`mailimap_uid_search`）
- `fetchLatestMessagesWithLimit:error:` - 快速获取最新邮件（`mailimap_uid_fetch`）
- `fetchMessageHeadersWithUID:error:` - 获取邮件头（`mailimap_uid_fetch`）
- `fetchBatchMessageHeadersWithUIDs:error:` - 批量获取邮件头（`mailimap_uid_fetch`）
- `fetchMessageBodyWithUID:error:` - 获取邮件正文（`mailimap_uid_fetch`）
- `markAsReadWithUID:error:` - 标记已读（`mailimap_uid_store`）

#### SMTP 操作：
- `connectWithError:` - SMTP 连接（`mailsmtp_ssl_connect`, `mailsmtp_socket_connect`, `mailsmtp_socket_starttls`）
- `loginWithError:` - SMTP 登录（`mailesmtp_ehlo`, `mailsmtp_helo`, `mailsmtp_auth`）
- `sendMessageTo:cc:bcc:subject:body:htmlBody:attachments:readReceipt:error:` - 发送邮件（`mailsmtp_mail`, `mailsmtp_rcpt`, `mailsmtp_data`）

## 技术细节

### 为什么需要设置线程 QoS？

在 macOS/iOS 中，线程的 QoS（Quality of Service）决定了线程的优先级和资源分配：

- **User Initiated**: 用户主动触发的操作，需要快速响应
- **Default**: 默认优先级，用于一般后台操作

当高优先级线程等待低优先级线程时，会发生优先级反转，导致：
- 高优先级线程被阻塞
- 系统性能下降
- Xcode/Instruments 报告警告

### 为什么在函数调用前设置？

LibEtPan 的 CFStream 实现会在 runloop 中等待网络事件。`wait_runloop` 函数会：
1. 调用 `CFRunLoopGetCurrent()` 获取当前线程的 runloop
2. 调用 `CFRunLoopRunInMode` 运行这个 runloop

如果我们在调用 LibEtPan 函数之前设置线程 QoS，那么：
- runloop 会运行在 User Initiated QoS 级别
- CFStream 的回调也会在正确的 QoS 级别执行
- 避免了优先级反转

## 测试建议

1. 使用 Xcode 的 Thread Sanitizer 运行应用，检查是否还有优先级反转警告
2. 使用 Instruments 的 Time Profiler 检查线程 QoS 设置是否正确
3. 测试所有邮件操作（连接、登录、获取邮件、发送邮件等），确保功能正常

## 相关文件

- `fastv/Services/LibEtPanWrapper.m` - 主要修改文件
- `fastv/Services/LibEtPanWrapper.h` - 头文件（未修改）
- `ThirdParty/libetpan/src/data-types/mailstream_cfstream.c` - LibEtPan 源码（第三方库，未修改）

## 参考文档

- [Apple: Quality of Service Classes](https://developer.apple.com/documentation/dispatch/dispatchqos)
- [Apple: Thread Sanitizer](https://developer.apple.com/documentation/xcode/diagnosing-memory-thread-and-crash-issues-early)
- [pthread_set_qos_class_self_np](https://developer.apple.com/documentation/kernel/1503664-pthread_set_qos_class_self_np)

