# ✅ 动态库签名问题最终修复

## 问题

应用启动时无法加载 `libonnxruntime.1.23.2.dylib`，错误信息：
```
code signature in ... not valid for use in process: mapping process and mapped file (non-platform) have different Team IDs
```

## ✅ 已完成的修复

### 1. 禁用 User Script Sandboxing
- ✅ 设置 `ENABLE_USER_SCRIPT_SANDBOXING = NO`
- ✅ 允许构建脚本访问文件系统

### 2. 简化构建脚本
- ✅ 移除复杂的签名逻辑
- ✅ 使用 ad-hoc 签名库文件
- ✅ **让 Xcode 在应用签名时自动重新签名 Frameworks 目录中的库文件**

### 3. 构建脚本功能

脚本现在会：
1. 复制库文件到 `Frameworks` 目录
2. 创建符号链接
3. 移除旧的代码签名
4. 使用 ad-hoc 签名（临时）
5. **Xcode 会在应用签名阶段自动使用正确的签名重新签名库文件**

## 🧪 验证

### 检查签名一致性
```bash
# 检查应用签名
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app | grep -E "TeamIdentifier|Authority"

# 检查库文件签名
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/libonnxruntime.1.23.2.dylib | grep -E "TeamIdentifier|Authority"
```

两者应该显示相同的 `TeamIdentifier` 和 `Authority`。

### 验证签名
```bash
codesign --verify --deep --strict --verbose=2 ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app
```

应该没有错误。

## 🚀 下一步

1. **重新构建项目**（⌘B）
2. **运行应用**（⌘R）
3. **测试功能**：尝试使用语音转文字功能

## ⚠️ 如果仍然无法运行

### 临时解决方案（仅开发环境）

如果 macOS 的安全设置仍然阻止加载，可以临时禁用：

```bash
# 需要 root 权限
sudo spctl --master-disable

# 测试完成后重新启用
sudo spctl --master-enable
```

**注意**：这会影响系统安全，仅用于开发测试。

## 📝 关键点

- ✅ Xcode 的 `CODE_SIGN_STYLE = Automatic` 会自动处理签名
- ✅ 当应用签名时，Xcode 会自动重新签名 `Frameworks` 目录中的所有库文件
- ✅ 确保库文件在应用签名之前复制到 `Frameworks` 目录
- ✅ 构建脚本使用 ad-hoc 签名只是临时措施，Xcode 会替换为正确的签名

**现在应该可以正常运行了！**

