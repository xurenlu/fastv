# ✅ 动态库加载问题修复总结

## 问题

应用启动时崩溃，错误信息：
```
dyld[11714]: Library not loaded: @rpath/libonnxruntime.1.23.2.dylib
Reason: code signature in ... not valid for use in process: mapping process and mapped file (non-platform) have different Team IDs
```

## ✅ 已完成的修复

### 1. 添加构建脚本
- ✅ 创建了 "Copy ONNX Runtime Library" 构建脚本
- ✅ 脚本在 Resources 阶段之后执行
- ✅ 自动复制库文件到 `fastv.app/Contents/Frameworks/`

### 2. 代码签名处理
- ✅ 移除库文件的旧代码签名
- ✅ 使用 ad-hoc 签名重新签名库文件
- ✅ **在构建脚本中自动重新签名整个应用**，确保签名一致

### 3. 符号链接创建
- ✅ 创建 `libonnxruntime.dylib` → `libonnxruntime.1.23.2.dylib`
- ✅ 确保链接器能找到库文件

## 📋 构建脚本功能

每次构建时，脚本会：
1. 复制 `libonnxruntime.1.23.2.dylib` 到 `Frameworks` 目录
2. 创建符号链接 `libonnxruntime.dylib`
3. 移除旧的代码签名
4. 使用 ad-hoc 签名重新签名库文件
5. **重新签名整个应用**，确保签名一致

## 🧪 验证

### 检查库文件
```bash
ls -lh ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/
```

### 检查代码签名
```bash
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app
codesign -dvv ~/Library/Developer/Xcode/DerivedData/fastv-*/Build/Products/Debug/fastv.app/Contents/Frameworks/libonnxruntime.1.23.2.dylib
```

两者都应该显示 `Signature=adhoc`

## 🚀 下一步

1. **重新构建项目**（⌘B）
2. **运行应用**（⌘R）
3. **测试功能**：尝试使用语音转文字功能

## ⚠️ 如果仍然无法运行

### 临时解决方案（仅开发环境）

如果 macOS 的安全设置阻止加载未签名的库，可以临时禁用：

```bash
# 需要 root 权限
sudo spctl --master-disable

# 测试完成后重新启用
sudo spctl --master-enable
```

**注意**：这会影响系统安全，仅用于开发测试。

## 📝 当前状态

- ✅ 库文件已复制到应用包
- ✅ 代码签名已处理（adhoc）
- ✅ 符号链接已创建
- ✅ 构建脚本已配置
- ✅ **应用会自动重新签名以确保一致性**

**现在可以重新构建并运行应用了！**

