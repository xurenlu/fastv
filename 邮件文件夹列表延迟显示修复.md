# 邮件文件夹列表延迟显示修复

## 🐛 问题描述

切换到邮件 tab 时，文件夹列表不能马上展示，界面卡了一下才出来。

## 🔍 问题分析

### 根本原因

1. **EmailStore 初始化是异步的**
   - `EmailStore.init()` 中，`loadFolders()` 是在 `Task` 中异步执行的
   - 当切换到邮件 tab 时，如果 `EmailStore` 还没加载完，文件夹列表就是空的

2. **EmailViewModel 依赖 EmailStore**
   - `EmailViewModel` 初始化时，订阅了 `emailStore.$folders`
   - 但如果 `EmailStore` 还没加载完，`folders` 就是空的，订阅不会立即触发更新

3. **初始化顺序问题**
   - `EmailView` 创建 `EmailViewModel` 时，`EmailStore` 可能还在初始化中
   - 导致文件夹列表延迟显示

## ✅ 修复方案

### 1. 优化 EmailStore 初始化优先级

**文件**: `EmailStore.swift`

```swift
private init() {
    // 立即加载账号和文件夹（高优先级，确保快速显示）
    Task.detached(priority: .userInitiated) { [weak self] in
        guard let self = self else { return }
        await self.loadAccounts()
        await self.loadFolders()
        
        // 预加载邮件缓存（低优先级，后台执行）
        Task.detached(priority: .background) { [weak self] in
            guard let self = self else { return }
            await self.preloadMessageCache()
        }
    }
}
```

**改进**:
- 使用 `userInitiated` 优先级，确保账号和文件夹快速加载
- 邮件缓存预加载使用 `background` 优先级，不阻塞文件夹列表显示

### 2. EmailViewModel 立即读取文件夹

**文件**: `EmailViewModel.swift`

**改进**:
1. 在 `init()` 中立即尝试读取文件夹（如果 EmailStore 已加载）
2. 如果文件夹为空，等待 EmailStore 加载完成（最多等待 1 秒）

```swift
// 初始化时立即尝试读取文件夹（如果 EmailStore 已加载）
if let accountId = selectedAccountId {
    folders = emailStore.getFolders(for: accountId)
} else if let defaultAccount = emailStore.getDefaultAccount() {
    selectedAccountId = defaultAccount.id
    folders = emailStore.getFolders(for: defaultAccount.id)
} else if let firstAccount = accounts.first {
    selectedAccountId = firstAccount.id
    folders = emailStore.getFolders(for: firstAccount.id)
}

// 如果文件夹为空，等待 EmailStore 加载完成
if folders.isEmpty {
    Task.detached(priority: .userInitiated) { [weak self] in
        guard let self = self else { return }
        // 等待 EmailStore 完成初始化（最多等待 1 秒）
        for _ in 0..<10 {
            try? await Task.sleep(nanoseconds: 100_000_000) // 0.1秒
            
            await MainActor.run {
                if let accountId = self.selectedAccountId {
                    let folders = self.emailStore.getFolders(for: accountId)
                    if !folders.isEmpty {
                        self.folders = folders
                        return // 找到了，退出循环
                    }
                }
            }
        }
    }
}
```

### 3. 优化 selectedAccountId 订阅

**文件**: `EmailViewModel.swift`

**改进**:
- 在 `$selectedAccountId` 订阅中，立即读取文件夹
- 如果文件夹为空，稍后重试

```swift
$selectedAccountId
    .sink { [weak self] accountId in
        guard let self = self else { return }
        if let accountId = accountId {
            // 立即从 EmailStore 读取文件夹（如果已加载）
            let folders = self.emailStore.getFolders(for: accountId)
            self.folders = folders
            
            // 如果文件夹为空，可能是 EmailStore 还在加载，稍后重试
            if folders.isEmpty {
                Task.detached(priority: .userInitiated) { [weak self] in
                    guard let self = self else { return }
                    // 等待一下，让 EmailStore 完成初始化
                    try? await Task.sleep(nanoseconds: 100_000_000) // 0.1秒
                    
                    // 再次尝试读取
                    await MainActor.run {
                        if let accountId = await self.selectedAccountId {
                            let folders = self.emailStore.getFolders(for: accountId)
                            if !folders.isEmpty {
                                self.folders = folders
                            }
                        }
                    }
                }
            }
        } else {
            self.folders = []
        }
    }
    .store(in: &cancellables)
```

## 📊 预期效果

### 修复前
- 切换到邮件 tab 时，文件夹列表延迟显示（0.5-2 秒）
- 界面卡顿，用户体验差

### 修复后
- 如果 EmailStore 已加载，文件夹列表立即显示（< 10ms）
- 如果 EmailStore 还在加载，最多等待 1 秒后显示
- 界面流畅，用户体验好

## 🧪 测试建议

1. **快速切换测试**
   - 快速切换到邮件 tab，检查文件夹列表是否立即显示

2. **冷启动测试**
   - 应用首次启动后，切换到邮件 tab，检查文件夹列表是否在 1 秒内显示

3. **多账号测试**
   - 有多个账号时，切换账号，检查文件夹列表是否立即更新

## 📝 注意事项

1. **EmailStore 是单例**
   - `EmailStore.shared` 在应用启动时就会初始化
   - 但初始化是异步的，可能需要一些时间

2. **数据库读取是异步的**
   - `loadFolders()` 使用 `asyncRead`，不会阻塞主线程
   - 但需要等待数据库操作完成

3. **重试机制**
   - 如果文件夹为空，会等待最多 1 秒
   - 每 0.1 秒检查一次，找到后立即退出

## 🔧 后续优化建议

1. **预加载策略**
   - 在应用启动时，提前初始化 `EmailStore`
   - 确保切换到邮件 tab 时，文件夹列表已经加载完成

2. **缓存机制**
   - 将文件夹列表缓存到内存中
   - 应用启动时立即从缓存读取，然后后台更新

3. **加载状态显示**
   - 如果文件夹列表为空，显示加载指示器
   - 让用户知道正在加载，而不是空白界面

---

**修复日期**: 2025-01-XX  
**修复人**: AI 代码评审专家

