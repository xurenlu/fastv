# 从零到一：打造一款高效的语音转文字输入法

## 前言

在数字化办公时代，语音输入已经成为提高效率的重要工具。今天我想分享我们是如何开发一款基于本地AI模型的语音转文字输入法，以及在这个过程中遇到的技术挑战和解决方案。

## 项目概述

我们的目标是开发一款**完全本地化**的语音输入法，具备以下特点：
- 实时语音识别，无需网络连接
- 低延迟，快速响应
- AI智能优化，自动去除口头禅和添加标点
- 隐私安全，所有数据在本地处理

## 核心技术架构

### 1. 语音识别模型：SenseVoice-Small

我们选择了 **SenseVoice-Small** 作为语音识别引擎，这是一个由阿里巴巴达摩院开源的多语言语音识别模型。

**为什么选择 SenseVoice？**
- **多语言支持**：支持超过50种语言，识别效果优于 Whisper
- **高效推理**：采用非自回归端到端框架，10秒音频仅需70ms，比 Whisper-Large 快15倍
- **本地部署**：可以完全离线运行，保护用户隐私
- **模型小巧**：Small版本模型体积适中，适合桌面应用

**技术实现：**

```swift
// 使用 ONNX Runtime 加载模型
let onnxWrapper = ONNXRuntimeWrapper()
try onnxWrapper.loadModel(from: modelPath.path)

// 提取 Mel 频谱特征
let features = try await AudioFeatureExtractor.extractMelFeatures(
    from: recording, 
    cmvnURL: cmvnURL
)

// 运行推理
let tokenIDs = try onnxWrapper.runInference(
    input: inputFeatures, 
    language: language
)
```

### 2. 内存录音：不写文件的实时录音

**挑战：**传统的语音输入应用通常需要先将录音保存为文件，然后再进行识别。这种方式存在以下问题：
- 磁盘I/O开销
- 文件管理复杂
- 延迟较高

**解决方案：**我们使用 `AVAudioEngine` 直接将音频数据捕获到内存中。

**核心实现：**

```swift
// 安装 tap 捕获音频数据
inputNode.installTap(onBus: 0, bufferSize: 1024, format: format) { [weak self] buffer, time in
    guard let self = self else { return }
    
    // 直接写入内存缓冲区
    if let channelData = buffer.floatChannelData {
        let frames = Int(buffer.frameLength)
        let byteCount = frames * MemoryLayout<Float>.size
        let pcmPointer = channelData.pointee
        let data = Data(bytes: pcmPointer, count: byteCount)
        
        // 使用队列安全地存储数据
        self.recordingDataQueue.async {
            self.recordedBuffers.append(data)
        }
    }
}
```

**关键点：**
- 使用 `DispatchQueue` 确保线程安全
- 使用 `[Data]` 数组存储音频块，避免频繁内存分配
- 录音结束后，将所有缓冲区合并为完整的音频数据

### 3. 采样率转换：实时转换为16kHz

**挑战：**不同设备的麦克风采样率可能不同（常见的有44.1kHz、48kHz等），但 SenseVoice 模型要求输入必须是 **16kHz 单声道 PCM** 格式。

**解决方案：**使用 `AVAudioConverter` 进行实时采样率转换。

**核心实现：**

```swift
private func convertPCMData(
    _ data: Data, 
    originalFormat: AVAudioFormat, 
    toSampleRate: Double, 
    toChannels: AVAudioChannelCount
) throws -> VoiceRecording {
    // 创建源缓冲区
    guard let sourceBuffer = AVAudioPCMBuffer(
        pcmFormat: originalFormat, 
        frameCapacity: AVAudioFrameCount(frames)
    ) else {
        throw VoiceInputError.failedToStartRecording("无法创建源缓冲")
    }
    
    // 创建目标格式（16kHz 单声道 Int16 PCM）
    guard let targetFormat = AVAudioFormat(
        commonFormat: .pcmFormatInt16,
        sampleRate: toSampleRate,
        channels: toChannels,
        interleaved: false
    ),
    let converter = AVAudioConverter(from: originalFormat, to: targetFormat) else {
        throw VoiceInputError.failedToStartRecording("无法创建转换器")
    }
    
    // 执行转换
    converter.convert(to: convertedBuffer, error: &error, withInputFrom: inputBlock)
    
    // 提取转换后的 PCM 数据
    let pcmData = Data(bytes: pcmPointer, count: byteCount)
    return VoiceRecording(pcmData: pcmData, sampleRate: toSampleRate, channelCount: Int(toChannels))
}
```

**技术细节：**
- 使用 `AVAudioConverter` 进行高质量的重采样
- 目标格式固定为 16kHz、单声道、16-bit PCM
- 转换后的数据直接存储在内存中，无需临时文件

### 4. AI文本优化：去除口头禅和水词

**挑战：**语音识别后的文本往往包含大量口头禅（如"嗯"、"那个"、"就是说"）和重复词，影响文本质量。

**解决方案：**集成 Ollama 本地AI服务，使用轻量级模型（如 gemma2:2b 或 gemma3:1b）进行文本优化。

**为什么选择本地AI？**
- **隐私保护**：文本不离开本地设备
- **低延迟**：本地推理延迟通常在2-5秒内
- **成本可控**：无需API调用费用
- **可定制**：可以调整提示词和模型参数

**核心实现：**

```swift
func optimizeTranscript(
    text: String,
    endpoint: String,
    model: String,
    apiToken: String?,
    timeout: TimeInterval = 5.0
) async throws -> String {
    // 构建系统提示词
    let systemPrompt = """
    你是一个专业的文本优化助手。你的任务是优化语音转文字的结果，使其更加清晰、准确、易读。
    
    请按以下要求处理文本：
    1. 去除口头禅：如"嗯"、"啊"、"呃"、"那个"、"就是说"等
    2. 去除无意义的语气词和填充词：如"哦"、"哎"、"诶"等
    3. 去除重复的词语：如"就就"、"这这"等口误
    4. 修正明显的错别字和同音字错误
    5. 添加适当的标点符号：句号、逗号、问号等
    6. 保持原文的意思、语气和风格不变
    """
    
    // 构建请求
    let requestBody: [String: Any] = [
        "model": model,
        "prompt": text,
        "system": systemPrompt,
        "stream": false,
        "options": [
            "temperature": 0.3,  // 较低温度使输出更稳定
            "top_p": 0.9
        ]
    ]
    
    // 发送请求到 Ollama
    let (data, response) = try await URLSession.shared.data(for: request)
    // ... 解析响应
}
```

**优化效果示例：**

**原始文本：**
```
嗯那个我今天就是说想要去那个超市买点东西然后呢顺便看看有没有什么优惠活动
```

**优化后：**
```
我今天想要去超市买点东西，顺便看看有没有什么优惠活动。
```

## 技术难点与解决方案

### 难点1：内存管理

**问题：**长时间录音可能导致内存占用过高。

**解决：**
- 使用固定大小的缓冲区（1024帧）
- 及时释放不再使用的音频数据
- 录音结束后立即清理缓冲区

### 难点2：线程安全

**问题：**音频回调在后台线程执行，需要安全地更新UI状态。

**解决：**
- 使用 `DispatchQueue` 进行数据同步
- 使用 `@MainActor` 标记UI更新方法
- 使用 `Task { @MainActor in }` 确保UI更新在主线程

### 难点3：采样率转换精度

**问题：**不同采样率之间的转换可能导致音频质量损失。

**解决：**
- 使用系统提供的 `AVAudioConverter`，它使用高质量的重采样算法
- 在转换前预留10%的缓冲区空间，避免数据截断
- 验证转换后的采样率和声道数

### 难点4：AI优化超时处理

**问题：**本地AI模型响应时间不确定，可能影响用户体验。

**解决：**
- 实现可配置的超时机制（默认5秒）
- AI优化失败时自动降级使用原始文本
- 提供测试功能，让用户评估响应速度

## 性能优化

### 1. 异步处理

所有耗时操作都使用 `async/await` 异步执行，不阻塞UI：

```swift
Task {
    let recording = try await voiceInputService.stopRecording()
    let text = try await SpeechTranscriber.transcribe(recording: recording)
    
    if preferences.enableAIOptimization {
        let optimizedText = try await OllamaService.shared.optimizeTranscript(
            text: text,
            endpoint: preferences.aiAPIEndpoint,
            model: preferences.aiModel,
            timeout: preferences.aiTimeout
        )
        text = optimizedText
    }
    
    textInsertion.insertText(text)
}
```

### 2. 模型缓存

- Token映射表在首次加载后缓存
- 避免重复读取配置文件

### 3. 音频特征提取优化

- 使用 Accelerate 框架进行高效的数学运算
- 批量处理音频帧，减少函数调用开销

## 技术栈总结

- **开发语言**：Swift
- **UI框架**：SwiftUI
- **音频处理**：AVFoundation
- **语音识别模型**：SenseVoice-Small (ONNX)
- **AI优化模型**：gemma2:2b / gemma3:1b (通过 Ollama)
- **推理引擎**：ONNX Runtime
- **特征提取**：Accelerate Framework

## 未来改进方向

1. **流式识别**：支持实时显示识别结果，而不是等待录音结束
2. **多模型支持**：允许用户选择不同的语音识别模型
3. **自定义优化规则**：允许用户自定义AI优化的提示词
4. **离线模型管理**：支持下载和管理多个AI模型
5. **性能监控**：添加性能指标监控，帮助用户优化设置

## 总结

通过这次开发，我们成功实现了一款完全本地化的语音转文字输入法。核心技术点包括：

1. **内存录音**：使用 AVAudioEngine 直接捕获音频到内存，避免文件I/O开销
2. **实时采样率转换**：使用 AVAudioConverter 将任意采样率转换为16kHz
3. **本地AI优化**：集成 Ollama 服务，使用轻量级模型优化文本质量

这些技术的结合，让我们在保证隐私安全的前提下，实现了低延迟、高质量的语音输入体验。

希望这篇文章对正在开发类似应用的开发者有所帮助。如果你有任何问题或建议，欢迎交流讨论！

---

**项目地址**：[GitHub链接]（待补充）

**技术文档**：更多技术细节请参考项目文档

