# macOS 应用分发签名说明

## 问题原因

当前使用的是 **"Apple Development"** 签名，这是**开发签名**，只能在本机或注册的设备上运行，不能用于分发给外部用户。

## 解决方案

### 1. 申请 Developer ID Application 证书

**Developer ID Application** 证书是用于分发给外部用户的必需证书。

#### 申请步骤：

1. **登录 Apple Developer 网站**
   - 访问：https://developer.apple.com/account
   - 使用您的 Apple Developer 账号登录

2. **创建证书**
   - 进入 "Certificates, Identifiers & Profiles"
   - 点击 "+" 创建新证书
   - 选择 "Developer ID Application"
   - 按照提示完成证书创建

3. **下载并安装证书**
   - 下载证书文件（.cer）
   - 双击安装到 Keychain
   - 或者在 Xcode 中自动下载：
     - Xcode > Preferences > Accounts
     - 选择您的 Apple ID
     - 点击 "Download Manual Profiles"

### 2. 验证证书安装

运行以下命令检查证书是否已安装：

```bash
security find-identity -v -p codesigning | grep "Developer ID"
```

应该看到类似输出：
```
"Developer ID Application: Your Name (TEAM_ID)"
```

### 3. 使用更新后的构建脚本

使用 `build_release_distribution.sh` 脚本，它会：
- 自动检测并使用 Developer ID 证书
- 签名应用和 DMG 文件
- 可选：进行公证（notarization）

### 4. 公证（Notarization）- 可选但推荐

公证可以让 macOS Gatekeeper 信任您的应用，用户安装时不会看到警告。

#### 公证步骤：

1. **创建 App-Specific Password**
   - 访问：https://appleid.apple.com
   - 登录 > App-Specific Passwords
   - 创建密码用于公证

2. **使用 altool 或 notarytool 进行公证**

```bash
# 使用 notarytool（推荐，macOS 13+）
xcrun notarytool submit typecho_v1.0.1.dmg \
  --apple-id "your-email@example.com" \
  --team-id "W49B66SUW3" \
  --password "app-specific-password" \
  --wait

# 或使用 altool（旧方法）
xcrun altool --notarize-app \
  --primary-bundle-id "com.wxside.typecho" \
  --username "your-email@example.com" \
  --password "app-specific-password" \
  --file typecho_v1.0.1.dmg
```

3. **装订公证票据**

```bash
xcrun stapler staple typecho_v1.0.1.dmg
```

## 签名类型对比

| 签名类型 | 用途 | 外部用户可用 |
|---------|------|------------|
| Apple Development | 开发测试 | ❌ 否 |
| Apple Distribution | App Store 分发 | ✅ 是（通过 App Store） |
| Developer ID Application | 直接分发 | ✅ 是（直接分发） |

## 当前状态

- ✅ 应用已使用 Apple Development 签名（仅开发用）
- ❌ DMG 文件未签名
- ❌ 缺少 Developer ID 证书（分发必需）

## 下一步

1. 申请 Developer ID Application 证书
2. 使用 `build_release_distribution.sh` 重新构建
3. （可选）进行公证

