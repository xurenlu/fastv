# AI 文本优化 - 快速开始

## 5 分钟快速上手

### 步骤 1：安装 Ollama（如果还没有安装）

```bash
# 使用 Homebrew 安装
brew install ollama
```

### 步骤 2：启动 Ollama 服务

```bash
ollama serve
```

保持这个终端窗口打开，让服务在后台运行。

### 步骤 3：下载模型

打开新的终端窗口，下载推荐的模型：

```bash
# 推荐：小巧快速的模型
ollama pull gemma2:2b

# 或者使用其他模型
ollama pull qwen2:1.5b
ollama pull llama3.2:1b
```

**注意**：首次下载模型可能需要几分钟，请耐心等待。

### 步骤 4：在 fastv 中启用 AI 优化

1. 打开 fastv 应用
2. 点击菜单栏的"设置"
3. 滚动到"AI 文本优化"部分
4. 勾选"启用 AI 文本优化"
5. 确认设置：
   - API 端点：`http://127.0.0.1:11434`（默认）
   - 模型名称：`gemma2:2b`（或你下载的模型名称）
   - API Token：留空（本地使用不需要）

### 步骤 5：调整超时时间（可选）

1. 使用滑块调整超时时间
2. 推荐设置：
   - 本地小模型（如 gemma2:2b）：2-5 秒
   - 本地大模型：5-10 秒

### 步骤 6：测试功能（推荐）

1. **测试连接**：点击"测试连接"按钮，确认 API 可访问
2. **测试文本优化**（重要）：
   - 点击"测试文本优化"按钮
   - 系统会发送测试文本并显示优化结果
   - 查看响应时间和速度评估
   - 根据结果调整超时设置

### 步骤 7：开始使用

1. 按下语音输入快捷键（默认：⌥V）
2. 说话
3. 松开快捷键
4. 等待几秒，优化后的文本会自动插入

## 效果对比示例

### 示例 1：去除口头禅

**原始：** 嗯那个我今天想去超市买点东西

**优化后：** 我今天想去超市买点东西。

### 示例 2：添加标点符号

**原始：** 我明天要去北京出差大概三天左右然后周五回来

**优化后：** 我明天要去北京出差，大概三天左右，然后周五回来。

### 示例 3：修正错别字

**原始：** 这个项目的进度还挺不错的我们因该能按时完成

**优化后：** 这个项目的进度还挺不错的，我们应该能按时完成。

## 效果测试

### 使用"测试文本优化"功能

点击"测试文本优化"按钮后，会看到类似这样的结果：

```
✅ 测试成功

⏱️ 响应时间: 2.34 秒 ✅ 正常

📝 原始文本:
嗯那个我今天想去超市买点东西然后呢顺便看看有没有什么优惠活动

✨ 优化后:
我今天想去超市买点东西，顺便看看有没有什么优惠活动。

💡 提示:
• 响应时间 < 5 秒：适合实时语音输入
• 响应时间 > 5 秒：建议使用更小的模型或调整超时设置
```

### 响应时间评估

- ⚡️ **< 2 秒**：非常快，体验极佳
- ✅ **2-5 秒**：正常，适合实时使用
- ⚠️ **5-10 秒**：较慢，可能影响体验
- ❌ **> 10 秒**：太慢，建议更换模型

## 常见问题

### Q: 为什么优化速度很慢？

A: 可能的原因：
- 使用的模型太大，建议使用 2B 以下的小模型
- 电脑性能不足，考虑升级硬件或使用更小的模型
- 首次使用时模型需要加载到内存，之后会快很多
- 超时时间设置太短，可以适当增加

**解决方法**：
1. 使用"测试文本优化"功能查看实际响应时间
2. 根据测试结果调整超时时间滑块
3. 尝试更小的模型（如 qwen2:1.5b）

### Q: 可以不使用 AI 优化吗？

A: 当然可以！AI 优化是可选功能，默认是关闭的。如果不需要，只需在设置中取消勾选即可。

### Q: 我的数据会发送到云端吗？

A: 不会！使用本地 Ollama 时，所有处理都在本机完成，数据不会离开你的电脑。

### Q: 可以使用其他 AI 服务吗？

A: 可以！只要是兼容 Ollama API 的服务都可以使用。修改 API 端点地址即可。

### Q: 推荐使用哪个模型？

A: 推荐使用（按速度排序）：
- **qwen2:1.5b** - 最快，中文支持好，响应时间约 1-2 秒
- **gemma2:2b** - 平衡性能和质量，响应时间约 2-3 秒
- **llama3.2:1b** - 很快，但中文支持一般

**提示**：使用"测试文本优化"功能对比不同模型的速度和效果！

### Q: 经常遇到超时怎么办？

A: 
1. 在设置中增加超时时间（使用滑块调整）
2. 使用"测试文本优化"查看实际需要的时间
3. 根据测试结果设置合适的超时值（建议比测试时间多 1-2 秒）
4. 如果还是超时，考虑更换更快的模型

## 高级配置

### 使用代理下载模型

如果下载模型很慢，可以设置代理：

```bash
export https_proxy=http://127.0.0.1:7890
export http_proxy=http://127.0.0.1:7890
ollama pull gemma2:2b
```

### 查看已安装的模型

```bash
ollama list
```

### 删除不需要的模型

```bash
ollama rm 模型名称
```

### 测试模型效果

在终端中测试模型：

```bash
ollama run gemma2:2b "优化这段文本：嗯那个我今天想去超市买点东西"
```

## 需要帮助？

如果遇到问题，请：
1. 查看控制台日志（应用菜单 > 查看 > 开发者工具）
2. 确认 Ollama 服务正在运行
3. 检查模型是否正确下载
4. 尝试重启应用

祝使用愉快！🎉

