# AI 文本优化功能说明

## 功能概述

为 fastv 语音输入法添加了 AI 文本优化功能，可以在语音识别完成后自动优化转录文本，去除口头禅、添加标点符号、修正错别字等。

## 主要特性

1. **可选启用**：默认不启用，用户需要在设置中手动开启
2. **灵活配置**：支持自定义 API 端点、模型名称和 API Token
3. **本地优先**：默认使用本机 Ollama 服务（http://127.0.0.1:11434）
4. **兼容性强**：支持任何兼容 Ollama API 的服务
5. **失败容错**：AI 优化失败时自动降级使用原始文本，不影响主流程

## 新增文件

### 1. OllamaService.swift
位置：`fastv/Services/OllamaService.swift`

主要功能：
- `optimizeTranscript()`: 优化转录文本
- `testConnection()`: 测试 API 连接
- `fetchModels()`: 获取可用模型列表

### 2. 用户偏好设置扩展
位置：`fastv/Models/UserPreferences.swift`

新增设置项：
- `enableAIOptimization`: 是否启用 AI 优化（默认：false）
- `aiAPIEndpoint`: API 端点地址（默认：http://127.0.0.1:11434）
- `aiModel`: 模型名称（默认：gemma2:2b）
- `aiAPIToken`: API Token（默认：空）
- `aiTimeout`: 超时时间（默认：5.0 秒）

## 设置界面

在设置页面新增"AI 文本优化"部分，包含：

1. **启用开关**：控制是否启用 AI 优化
2. **API 端点**：配置 Ollama 服务地址
3. **模型名称**：指定使用的模型
4. **API Token**：可选的认证令牌
5. **超时设置**：可调节的超时时间（2-30 秒），带滑块控制
6. **测试连接**：验证 API 连接是否正常
7. **获取模型列表**：从 Ollama 获取可用模型
8. **测试文本优化**：发送实际文本测试优化效果和响应速度

## 使用流程

### 1. 启用功能

1. 打开"设置"
2. 找到"AI 文本优化"部分
3. 勾选"启用 AI 文本优化"
4. 配置 API 端点和模型（可选，使用默认值即可）

### 2. 配置超时时间

1. 使用滑块调整超时时间（2-30 秒）
2. 建议设置：
   - 本地模型：2-5 秒
   - 远程 API：10-30 秒

### 3. 测试连接

1. 确保本地 Ollama 服务正在运行
2. 点击"测试连接"按钮
3. 如果连接成功，会显示成功提示

### 4. 测试文本优化（推荐）

1. 点击"测试文本优化"按钮
2. 系统会发送测试文本进行实际优化
3. 查看优化结果和响应时间
4. 根据响应时间调整超时设置

### 5. 获取模型列表

1. 点击"获取模型列表"按钮
2. 查看可用的模型列表
3. 将想要使用的模型名称复制到"模型名称"字段

### 6. 使用语音输入

1. 按下快捷键开始语音输入
2. 松开快捷键后，系统会：
   - 停止录音
   - 进行语音识别
   - **自动调用 AI 优化文本**（如果启用）
   - 插入优化后的文本

## AI 优化效果

AI 会对转录文本进行以下优化：

1. **去除口头禅**：如"嗯"、"啊"、"呃"、"那个"、"就是说"等
2. **去除语气词**：如"哦"、"哎"、"诶"等
3. **去除重复词**：如"就就"、"这这"等口误
4. **修正错别字**：修正明显的同音字错误
5. **添加标点符号**：自动添加句号、逗号、问号等
6. **保持原意**：不改变原文的意思和语气

### 示例

**原始文本：**
```
嗯那个我今天就是说想要去那个超市买点东西然后呢顺便看看有没有什么优惠活动
```

**优化后：**
```
我今天想要去超市买点东西，顺便看看有没有什么优惠活动。
```

## 配置说明

### 默认配置

```
API 端点: http://127.0.0.1:11434
模型: gemma2:2b
API Token: (空)
超时时间: 5.0 秒
```

### 使用本地 Ollama

1. 安装 Ollama：
   ```bash
   brew install ollama
   ```

2. 启动 Ollama 服务：
   ```bash
   ollama serve
   ```

3. 下载模型（推荐使用小模型以获得更快的响应）：
   ```bash
   ollama pull gemma2:2b
   # 或其他模型
   ollama pull qwen2:1.5b
   ```

### 使用外部 API

如果要使用外部的 Ollama 兼容 API：

1. 修改 API 端点为外部地址
2. 如果需要认证，填写 API Token
3. 点击"测试连接"验证配置

## 系统提示词

AI 使用以下系统提示词来优化文本：

```
你是一个专业的文本优化助手。你的任务是优化语音转文字的结果，使其更加清晰、准确、易读。

请按以下要求处理文本：
1. 去除口头禅：如"嗯"、"啊"、"呃"、"那个"、"就是说"、"然后呢"、"这个"、"怎么说呢"等
2. 去除无意义的语气词和填充词：如"哦"、"哎"、"诶"等
3. 去除重复的词语：如"就就"、"这这"等口误
4. 修正明显的错别字和同音字错误
5. 添加适当的标点符号：句号、逗号、问号、感叹号、顿号等
6. 保持原文的意思、语气和风格不变
7. 不要添加原文中没有的内容
8. 不要过度修改，保持口语化的自然感

请直接返回优化后的文本，不要添加任何解释、说明或额外的内容。
```

## 技术细节

### API 请求格式

```json
{
  "model": "gemma2:2b",
  "prompt": "原始转录文本",
  "system": "系统提示词",
  "stream": false,
  "format": "json",
  "options": {
    "temperature": 0.3,
    "top_p": 0.9
  }
}
```

### 错误处理

- AI 优化失败时，自动使用原始转录文本
- 不会因为 AI 服务不可用而影响语音输入功能
- 所有错误都会记录到控制台日志

### 性能考虑

- AI 优化是异步执行的，不会阻塞 UI
- 使用较低的 temperature (0.3) 以获得更稳定的输出
- 建议使用小模型（如 gemma2:2b）以获得更快的响应

## 超时时间设置

### 推荐设置

| 场景 | 推荐超时 | 说明 |
|------|---------|------|
| 本地小模型（< 3B） | 2-3 秒 | 快速响应，适合实时输入 |
| 本地中等模型（3-7B） | 3-5 秒 | 平衡质量和速度 |
| 本地大模型（> 7B） | 5-10 秒 | 更好的质量，但较慢 |
| 远程 API | 10-30 秒 | 考虑网络延迟 |

### 响应时间评估

测试文本优化功能会显示实际响应时间和评估：

- ⚡️ **< 2 秒**：非常快，体验极佳
- ✅ **2-5 秒**：正常，适合实时使用
- ⚠️ **5-10 秒**：较慢，可能影响体验
- ❌ **> 10 秒**：太慢，建议更换模型或调整设置

## 注意事项

1. **首次使用**：需要先下载 Ollama 模型，可能需要几分钟到几十分钟
2. **网络要求**：使用外部 API 时需要稳定的网络连接
3. **隐私保护**：使用本地 Ollama 可以确保数据不离开本机
4. **性能影响**：AI 优化会增加处理时间（取决于模型、硬件和超时设置）
5. **超时处理**：如果超时，会自动使用原始转录文本，不影响主流程

## 故障排查

### 连接失败

1. 检查 Ollama 服务是否正在运行
2. 验证 API 端点地址是否正确
3. 如果使用外部 API，检查网络连接和防火墙设置

### 优化效果不佳

1. 尝试使用不同的模型
2. 调整系统提示词（需要修改代码）
3. 检查原始转录文本的质量

### 响应速度慢

1. **调整超时时间**：增加超时时间以适应较慢的模型
2. **使用更小的模型**：如 qwen2:1.5b、llama3.2:1b
3. **优化硬件**：确保 Ollama 使用了 GPU 加速
4. **测试不同模型**：使用"测试文本优化"功能对比不同模型的速度

### 经常超时

1. **增加超时时间**：在设置中调整超时滑块
2. **检查系统资源**：确保 CPU/GPU 没有被其他程序占用
3. **更换模型**：使用更小、更快的模型
4. **测试网络**：如果使用远程 API，检查网络连接

## 未来改进方向

1. **可配置的提示词**：允许用户自定义系统提示词
2. **多种优化模式**：提供不同的优化风格（正式、口语化等）
3. **批量优化**：支持对历史记录进行批量优化
4. **优化对比**：显示优化前后的对比
5. **性能优化**：支持流式输出以减少等待时间

## 更新日志

### 2025-11-21 v1.1
- 添加超时时间设置（2-30 秒可调）
- 添加"测试文本优化"功能，可以实际测试优化效果和响应速度
- 优化测试结果显示，包含响应时间评估和速度建议
- 改进日志输出，显示 AI 优化耗时

### 2025-11-21 v1.0
- 初始版本发布
- 支持基本的文本优化功能
- 支持自定义 API 端点和模型
- 添加连接测试和模型列表获取功能

