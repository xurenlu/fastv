# 邮箱功能完善计划

## 📋 总体目标

一次性完成所有缺失的邮箱客户端功能，打造功能完整的专业邮箱客户端。

## 🎯 实施策略

1. **模块化开发**：按功能模块划分，每个模块独立完成
2. **渐进式实现**：先实现核心功能，再完善高级特性
3. **统一架构**：遵循现有代码风格和架构模式
4. **用户体验优先**：确保每个功能都有良好的UI和交互

---

## 📦 模块划分

### 模块 1：邮件签名系统 ⭐⭐⭐⭐⭐
**优先级：最高 | 预计工作量：1-2天**

#### 功能需求
- [ ] 多账号签名支持（每个账号可设置不同签名）
- [ ] HTML/纯文本签名
- [ ] 签名模板管理（创建/编辑/删除）
- [ ] 签名变量替换（姓名、邮箱、日期等）
- [ ] 默认签名设置
- [ ] 签名预览功能

#### 技术实现
1. **数据模型**
   - `EmailSignature` 模型
   - 数据库表：`email_signatures`
   - 关联账号ID

2. **服务层**
   - `EmailSignatureService` - 签名管理服务
   - 签名变量替换引擎
   - 签名渲染（HTML/文本）

3. **UI界面**
   - 签名管理界面（设置页面）
   - 编写窗口签名选择器
   - 签名预览组件

4. **集成点**
   - `EmailComposeWindowView` - 自动插入签名
   - `EmailService.sendMessage` - 发送时添加签名

---

### 模块 2：邮件规则管理 UI ⭐⭐⭐⭐⭐
**优先级：最高 | 预计工作量：2-3天**

#### 功能需求
- [ ] 规则列表界面（显示所有规则）
- [ ] 规则创建/编辑界面（可视化表单）
- [ ] 规则条件设置（发件人、主题、关键词等）
- [ ] 规则动作设置（移动、标记、转发等）
- [ ] 规则测试功能（测试规则是否匹配）
- [ ] 规则启用/禁用开关
- [ ] 规则执行日志

#### 技术实现
1. **数据模型**
   - `EmailRule` 模型（扩展现有）
   - 规则条件/动作的序列化

2. **服务层**
   - `EmailRuleService` - 规则管理服务
   - 规则验证和测试
   - 规则执行引擎（集成现有Lua引擎）

3. **UI界面**
   - `EmailRulesView` - 规则管理主界面
   - `EmailRuleEditorView` - 规则编辑界面
   - `EmailRuleTesterView` - 规则测试界面

4. **集成点**
   - `EmailViewModel` - 新邮件时自动执行规则
   - `EmailService.syncMessages` - 同步时应用规则

---

### 模块 3：邮件过滤/自动分类 ⭐⭐⭐⭐
**优先级：高 | 预计工作量：2天**

#### 功能需求
- [ ] 基于规则的自动分类
- [ ] 按发件人自动移动到文件夹
- [ ] 按主题关键词自动分类
- [ ] 自动标记/标签
- [ ] 自动转发规则
- [ ] 分类统计和报告

#### 技术实现
1. **服务层**
   - `EmailFilterService` - 过滤服务
   - 集成到规则引擎

2. **UI界面**
   - 在规则管理界面中实现
   - 分类统计视图

3. **集成点**
   - `EmailService.syncMessages` - 同步时自动分类
   - `EmailViewModel` - 新邮件时自动分类

---

### 模块 4：邮件线程/会话视图 ⭐⭐⭐⭐⭐
**优先级：最高 | 预计工作量：3-4天**

#### 功能需求
- [ ] 按主题/Message-ID分组邮件
- [ ] 会话折叠/展开视图
- [ ] 会话内邮件关系可视化
- [ ] 会话统计（邮件数量、参与者等）
- [ ] 会话操作（标记整个会话、删除会话等）

#### 技术实现
1. **数据模型**
   - `EmailThread` 模型
   - 线程关系计算（基于Message-ID、In-Reply-To、References）
   - 数据库表：`email_threads`

2. **服务层**
   - `EmailThreadService` - 线程管理服务
   - 线程关系解析算法
   - 线程更新逻辑

3. **UI界面**
   - `EmailThreadView` - 线程视图组件
   - 线程列表视图
   - 线程详情视图（展开的会话）

4. **集成点**
   - `EmailViewModel` - 添加线程视图模式
   - `EmailView` - 切换列表/线程视图
   - `EmailService.syncMessages` - 同步时建立线程关系

---

### 模块 5：邮件标签管理 UI ⭐⭐⭐⭐
**优先级：高 | 预计工作量：1-2天**

#### 功能需求
- [ ] 标签列表管理（创建/编辑/删除）
- [ ] 标签颜色设置
- [ ] 标签图标选择
- [ ] 批量应用标签
- [ ] 标签筛选功能
- [ ] 标签统计

#### 技术实现
1. **数据模型**
   - `EmailTag` 模型（扩展现有tags）
   - 数据库表：`email_tags`

2. **服务层**
   - `EmailTagService` - 标签管理服务
   - 标签应用逻辑

3. **UI界面**
   - `EmailTagsView` - 标签管理界面
   - 标签选择器组件
   - 标签显示组件（带颜色）

4. **集成点**
   - `EmailViewModel` - 标签操作
   - `EmailView` - 标签显示和筛选
   - `EmailMessage` - 标签数据模型

---

### 模块 6：邮件模板/快速回复 ⭐⭐⭐
**优先级：中 | 预计工作量：1-2天**

#### 功能需求
- [ ] 模板列表管理
- [ ] 模板创建/编辑（支持变量）
- [ ] 模板分类（常用、工作、个人等）
- [ ] 快速插入模板
- [ ] 模板变量替换（姓名、日期、主题等）

#### 技术实现
1. **数据模型**
   - `EmailTemplate` 模型
   - 数据库表：`email_templates`

2. **服务层**
   - `EmailTemplateService` - 模板管理服务
   - 模板变量替换引擎

3. **UI界面**
   - `EmailTemplatesView` - 模板管理界面
   - 模板选择器（编写窗口）
   - 模板编辑器

4. **集成点**
   - `EmailComposeWindowView` - 模板插入功能
   - `EmailViewModel` - 模板管理

---

### 模块 7：邮件归档功能 ⭐⭐⭐
**优先级：中 | 预计工作量：1天**

#### 功能需求
- [ ] 一键归档按钮
- [ ] 自动归档规则
- [ ] 归档文件夹管理
- [ ] 归档恢复功能

#### 技术实现
1. **服务层**
   - `EmailArchiveService` - 归档服务
   - 集成到规则引擎

2. **UI界面**
   - 归档按钮（邮件操作栏）
   - 归档设置界面

3. **集成点**
   - `EmailViewModel` - 归档操作
   - `EmailService` - 归档邮件移动

---

### 模块 8：邮件标记为重要（UI） ⭐⭐⭐
**优先级：中 | 预计工作量：0.5天**

#### 功能需求
- [ ] 重要标记按钮（UI）
- [ ] 重要邮件筛选
- [ ] 重要邮件统计

#### 技术实现
1. **UI界面**
   - 重要标记按钮（邮件操作栏）
   - 重要邮件筛选器

2. **集成点**
   - `EmailViewModel` - 标记重要操作
   - `EmailView` - 重要邮件显示

---

### 模块 9：邮件定时发送 ⭐⭐⭐
**优先级：中 | 预计工作量：2天**

#### 功能需求
- [ ] 定时发送设置（日期时间选择）
- [ ] 延迟发送（几分钟后）
- [ ] 定时发送队列管理
- [ ] 定时发送通知
- [ ] 取消定时发送

#### 技术实现
1. **数据模型**
   - `ScheduledEmail` 模型
   - 数据库表：`scheduled_emails`

2. **服务层**
   - `EmailSchedulerService` - 定时发送服务
   - 后台任务调度器
   - 定时检查和执行

3. **UI界面**
   - 定时发送设置界面（编写窗口）
   - 定时发送队列视图

4. **集成点**
   - `EmailComposeWindowView` - 定时发送选项
   - `EmailService` - 定时发送执行

---

### 模块 10：邮件批量操作 ⭐⭐⭐
**优先级：中 | 预计工作量：1-2天**

#### 功能需求
- [ ] 多选邮件（复选框）
- [ ] 批量标记已读/未读
- [ ] 批量删除
- [ ] 批量移动
- [ ] 批量应用标签
- [ ] 批量导出

#### 技术实现
1. **UI界面**
   - 邮件列表多选模式
   - 批量操作工具栏
   - 批量操作确认对话框

2. **服务层**
   - `EmailBatchService` - 批量操作服务

3. **集成点**
   - `EmailView` - 多选模式切换
   - `EmailViewModel` - 批量操作逻辑

---

### 模块 11：邮件打印功能 ⭐⭐
**优先级：低 | 预计工作量：1天**

#### 功能需求
- [ ] 打印预览
- [ ] 打印格式设置（页眉页脚、字体等）
- [ ] 打印样式选择

#### 技术实现
1. **UI界面**
   - 打印预览窗口
   - 打印设置界面

2. **服务层**
   - `EmailPrintService` - 打印服务
   - HTML到PDF转换

3. **集成点**
   - `EmailView` - 打印按钮
   - `EmailViewModel` - 打印操作

---

### 模块 12：联系人管理 ⭐⭐⭐
**优先级：中 | 预计工作量：2-3天**

#### 功能需求
- [ ] 联系人列表（从邮件自动提取）
- [ ] 联系人详情（姓名、邮箱、备注等）
- [ ] 联系人分组
- [ ] 联系人搜索
- [ ] 联系人头像/备注
- [ ] 从联系人快速发邮件

#### 技术实现
1. **数据模型**
   - `EmailContact` 模型（扩展现有）
   - 数据库表：`email_contacts`

2. **服务层**
   - `EmailContactService` - 联系人管理服务
   - 自动提取联系人（从收件人/发件人）

3. **UI界面**
   - `EmailContactsView` - 联系人管理界面
   - 联系人详情视图
   - 联系人选择器（编写窗口）

4. **集成点**
   - `EmailService` - 自动提取联系人
   - `EmailComposeWindowView` - 联系人选择
   - `EmailView` - 联系人快速操作

---

### 模块 13：IMAP IDLE 实时推送 ⭐⭐⭐
**优先级：中 | 预计工作量：2-3天**

#### 功能需求
- [ ] IMAP IDLE 连接保持
- [ ] 实时新邮件通知
- [ ] 连接状态监控
- [ ] 自动重连机制

#### 技术实现
1. **服务层**
   - `EmailPushService` - 推送服务
   - IMAP IDLE 实现（LibEtPan）
   - 后台连接管理

2. **集成点**
   - `EmailService` - IDLE连接
   - `EmailViewModel` - 实时更新

---

### 模块 14：文件夹颜色标记 ⭐⭐
**优先级：低 | 预计工作量：0.5天**

#### 功能需求
- [ ] 文件夹颜色设置
- [ ] 颜色选择器
- [ ] 文件夹列表显示颜色

#### 技术实现
1. **数据模型**
   - `EmailFolder` 模型扩展（color字段）

2. **UI界面**
   - 文件夹颜色选择器
   - 文件夹列表显示颜色

3. **集成点**
   - `EmailView` - 文件夹列表
   - `EmailViewModel` - 文件夹管理

---

## 📅 实施时间表

### 第一阶段：核心功能（5-7天）
1. **模块 1：邮件签名系统** (1-2天)
2. **模块 4：邮件线程/会话视图** (3-4天)
3. **模块 2：邮件规则管理 UI** (2-3天)

### 第二阶段：重要功能（4-5天）
4. **模块 3：邮件过滤/自动分类** (2天)
5. **模块 5：邮件标签管理 UI** (1-2天)
6. **模块 8：邮件标记为重要（UI）** (0.5天)
7. **模块 7：邮件归档功能** (1天)

### 第三阶段：效率提升（4-5天）
8. **模块 6：邮件模板/快速回复** (1-2天)
9. **模块 9：邮件定时发送** (2天)
10. **模块 10：邮件批量操作** (1-2天)

### 第四阶段：高级功能（4-5天）
11. **模块 12：联系人管理** (2-3天)
12. **模块 13：IMAP IDLE 实时推送** (2-3天)

### 第五阶段：完善功能（2-3天）
13. **模块 11：邮件打印功能** (1天)
14. **模块 14：文件夹颜色标记** (0.5天)

**总计：约 19-25 个工作日**

---

## 🏗️ 架构设计

### 新增服务层
```
fastv/Services/
├── EmailSignatureService.swift      # 签名管理
├── EmailRuleService.swift            # 规则管理（扩展）
├── EmailFilterService.swift          # 过滤服务
├── EmailThreadService.swift          # 线程管理
├── EmailTagService.swift             # 标签管理
├── EmailTemplateService.swift        # 模板管理
├── EmailArchiveService.swift         # 归档服务
├── EmailSchedulerService.swift       # 定时发送
├── EmailBatchService.swift           # 批量操作
├── EmailPrintService.swift           # 打印服务
├── EmailContactService.swift         # 联系人管理
└── EmailPushService.swift            # 推送服务
```

### 新增数据模型
```
fastv/Models/
├── EmailSignature.swift              # 签名模型
├── EmailRule.swift                   # 规则模型（扩展）
├── EmailThread.swift                 # 线程模型
├── EmailTag.swift                    # 标签模型
├── EmailTemplate.swift               # 模板模型
├── ScheduledEmail.swift              # 定时邮件模型
└── EmailContact.swift                # 联系人模型（扩展）
```

### 新增视图
```
fastv/Views/
├── EmailSignature/
│   ├── EmailSignatureView.swift      # 签名管理界面
│   └── EmailSignatureEditorView.swift # 签名编辑界面
├── EmailRules/
│   ├── EmailRulesView.swift          # 规则管理界面
│   ├── EmailRuleEditorView.swift     # 规则编辑界面
│   └── EmailRuleTesterView.swift     # 规则测试界面
├── EmailThread/
│   ├── EmailThreadView.swift         # 线程视图
│   └── EmailThreadDetailView.swift   # 线程详情
├── EmailTags/
│   ├── EmailTagsView.swift           # 标签管理界面
│   └── EmailTagSelectorView.swift    # 标签选择器
├── EmailTemplates/
│   ├── EmailTemplatesView.swift      # 模板管理界面
│   └── EmailTemplateEditorView.swift # 模板编辑界面
├── EmailContacts/
│   ├── EmailContactsView.swift       # 联系人管理界面
│   └── EmailContactDetailView.swift  # 联系人详情
└── EmailBatch/
    └── EmailBatchOperationView.swift # 批量操作视图
```

---

## 🔧 数据库扩展

### 新增表结构
```sql
-- 邮件签名表
CREATE TABLE email_signatures (
    id TEXT PRIMARY KEY,
    account_id TEXT NOT NULL,
    name TEXT NOT NULL,
    content TEXT NOT NULL,
    is_html INTEGER NOT NULL DEFAULT 0,
    is_default INTEGER NOT NULL DEFAULT 0,
    created_at REAL NOT NULL,
    updated_at REAL NOT NULL
);

-- 邮件线程表
CREATE TABLE email_threads (
    id TEXT PRIMARY KEY,
    account_id TEXT NOT NULL,
    subject TEXT,
    message_id TEXT,
    root_message_id TEXT,
    participant_emails TEXT,
    message_count INTEGER NOT NULL DEFAULT 0,
    unread_count INTEGER NOT NULL DEFAULT 0,
    last_message_date REAL NOT NULL,
    created_at REAL NOT NULL,
    updated_at REAL NOT NULL
);

-- 邮件标签表
CREATE TABLE email_tags (
    id TEXT PRIMARY KEY,
    account_id TEXT,
    name TEXT NOT NULL,
    color TEXT,
    icon TEXT,
    created_at REAL NOT NULL
);

-- 邮件模板表
CREATE TABLE email_templates (
    id TEXT PRIMARY KEY,
    account_id TEXT,
    name TEXT NOT NULL,
    category TEXT,
    content TEXT NOT NULL,
    variables TEXT,
    created_at REAL NOT NULL,
    updated_at REAL NOT NULL
);

-- 定时邮件表
CREATE TABLE scheduled_emails (
    id TEXT PRIMARY KEY,
    account_id TEXT NOT NULL,
    to_addresses TEXT NOT NULL,
    cc_addresses TEXT,
    bcc_addresses TEXT,
    subject TEXT NOT NULL,
    body TEXT NOT NULL,
    html_body TEXT,
    attachments TEXT,
    scheduled_date REAL NOT NULL,
    status TEXT NOT NULL,
    created_at REAL NOT NULL
);

-- 联系人表（扩展）
ALTER TABLE email_contacts ADD COLUMN notes TEXT;
ALTER TABLE email_contacts ADD COLUMN avatar_path TEXT;
ALTER TABLE email_contacts ADD COLUMN groups TEXT;
ALTER TABLE email_contacts ADD COLUMN last_contact_date REAL;
```

---

## ✅ 实施检查清单

### 代码质量
- [ ] 所有新代码遵循现有代码风格
- [ ] 添加必要的注释和文档
- [ ] 错误处理完善
- [ ] 异步操作正确处理
- [ ] 内存管理正确（避免循环引用）

### 用户体验
- [ ] 所有功能都有清晰的UI
- [ ] 操作反馈及时（加载状态、成功/失败提示）
- [ ] 快捷键支持（如需要）
- [ ] 帮助提示完善

### 性能优化
- [ ] 数据库操作优化（批量操作、索引）
- [ ] 网络请求优化（避免重复请求）
- [ ] UI渲染优化（避免卡顿）
- [ ] 内存使用优化（及时释放资源）

### 测试
- [ ] 功能测试（每个功能都测试）
- [ ] 边界情况测试
- [ ] 错误情况测试
- [ ] 性能测试（大量数据）

---

## 🚀 开始实施

建议按照以下顺序开始：

1. **先完成模块 1（邮件签名）** - 相对独立，容易验证
2. **然后完成模块 4（邮件线程）** - 核心功能，影响大
3. **接着完成模块 2（规则管理UI）** - 完善现有功能
4. **按优先级依次完成其他模块**

每个模块完成后，建议：
- 提交代码并测试
- 更新文档
- 检查是否有遗漏

---

## 📝 注意事项

1. **向后兼容**：确保新功能不影响现有功能
2. **数据迁移**：如有数据库结构变更，需要迁移脚本
3. **性能考虑**：大量邮件时的性能表现
4. **用户体验**：保持界面简洁，功能易用
5. **错误处理**：网络错误、数据错误等都要妥善处理

---

## 🎯 完成标准

所有模块完成后，邮箱客户端应具备：
- ✅ 完整的邮件收发功能
- ✅ 强大的邮件管理功能（规则、过滤、分类）
- ✅ 优秀的用户体验（线程视图、标签、模板）
- ✅ 高效的邮件处理（批量操作、定时发送）
- ✅ 完善的辅助功能（联系人、打印、归档）

---

**计划制定日期：** 2025年1月
**预计完成时间：** 19-25个工作日
**负责人：** 开发团队

