# 清理提醒事项权限缓存

## 问题说明

提醒事项权限窗口无法弹出，通常是因为 macOS 的 TCC (Transparency, Consent, and Control) 数据库缓存了权限状态。

## 解决方案

### 方案 1：使用 tccutil 重置权限（推荐）⭐

这是最简单的方法，不需要禁用 SIP：

```bash
# 重置提醒事项权限（需要替换为你的 Bundle ID）
tccutil reset Reminders com.wxside.typecho
```

**步骤**：
1. 打开终端
2. 执行上述命令（替换 Bundle ID）
3. 重启应用
4. 再次尝试请求权限

**注意**：如果应用名称不是 "typecho"，需要替换为实际的 Bundle ID。

---

### 方案 2：手动清理系统设置

1. 打开"系统设置"
2. 进入"隐私与安全性"
3. 选择"提醒事项"
4. 找到你的应用（可能是 "typecho" 或 "妙打"）
5. 取消勾选（如果已勾选）
6. 关闭系统设置
7. 重启应用
8. 再次尝试请求权限

---

### 方案 3：更改 Bundle ID（最彻底）⭐

如果上述方法都不行，更改 Bundle ID 会创建一个全新的应用标识，权限状态会完全重置。

#### 步骤 1：在 Xcode 中更改 Bundle ID

1. 打开 `fastv.xcodeproj`
2. 选择 Target "typecho"
3. 进入 "Signing & Capabilities"
4. 修改 Bundle Identifier：
   - 从 `com.wxside.typecho` 
   - 改为 `com.wxside.miaoda`（或其他唯一标识）

#### 步骤 2：更新 project.pbxproj

需要更新以下位置：
- `PRODUCT_BUNDLE_IDENTIFIER` 的值

#### 步骤 3：清理并重新构建

```bash
# 清理构建缓存
rm -rf ~/Library/Developer/Xcode/DerivedData/fastv-*

# 在 Xcode 中重新构建
```

---

### 方案 4：使用 SQLite 直接修改 TCC 数据库（需要禁用 SIP）⚠️

⚠️ **不推荐**：需要禁用系统完整性保护（SIP），存在安全风险。

如果必须使用：

1. 重启到恢复模式（Command+R）
2. 打开终端
3. 执行：`csrutil disable`
4. 重启
5. 执行以下命令（替换 Bundle ID）：

```bash
# 删除提醒事项权限记录
sudo sqlite3 ~/Library/Application\ Support/com.apple.TCC/TCC.db \
  "DELETE FROM access WHERE service='kTCCServiceReminders' AND client='com.wxside.typecho';"
```

6. 重启到恢复模式
7. 执行：`csrutil enable`
8. 重启

---

## 检查当前权限状态

### 方法 1：查看系统设置

1. 打开"系统设置"
2. 进入"隐私与安全性"
3. 选择"提醒事项"
4. 查看应用是否在列表中

### 方法 2：使用命令行检查

```bash
# 检查 TCC 数据库中的权限记录
sqlite3 ~/Library/Application\ Support/com.apple.TCC/TCC.db \
  "SELECT * FROM access WHERE service='kTCCServiceReminders' AND client LIKE '%typecho%';"
```

### 方法 3：在应用中打印权限状态

在 `RemindersSyncService.swift` 中添加调试代码：

```swift
let status = EKEventStore.authorizationStatus(for: .reminder)
print("📋 [RemindersSyncService] 当前权限状态: \(status.rawValue)")
// 0 = notDetermined, 1 = restricted, 2 = denied, 3 = authorized, 4 = fullAccess
```

---

## 推荐的解决流程

1. **首先尝试方案 1**（tccutil 重置）
   - 最简单，不需要修改代码
   - 如果不行，继续下一步

2. **尝试方案 2**（手动清理）
   - 检查系统设置中是否有权限记录
   - 清理后重启应用

3. **如果还是不行，使用方案 3**（更改 Bundle ID）
   - 这是最彻底的解决方案
   - 会创建一个全新的应用标识
   - 需要重新签名和分发

---

## 预防措施

为了避免将来再次遇到这个问题：

1. **在开发阶段使用不同的 Bundle ID**
   - Debug: `com.wxside.typecho.debug`
   - Release: `com.wxside.typecho`

2. **测试权限请求流程**
   - 在干净的系统中测试
   - 确保权限请求对话框能正常弹出

3. **提供清晰的用户引导**
   - 如果权限被拒绝，引导用户到系统设置中手动授权
   - 提供详细的权限说明

---

## 相关文件

- `fastv/Services/RemindersSyncService.swift` - 权限请求实现
- `fastv/Info.plist` - 权限说明配置
- `fastv.xcodeproj/project.pbxproj` - Bundle ID 配置

