# 如何编辑 SMTP 配置

## 问题

Gmail SMTP 使用 STARTTLS（端口 587）时出现错误代码 24，需要改为 SSL 直接连接（端口 465）。

## 解决方案

现在可以在邮箱账号管理中编辑 SMTP 配置了！

### 操作步骤

1. **打开邮箱账号管理**
   - 在邮箱主界面，点击工具栏的"账号管理"按钮

2. **编辑账号**
   - 在账号列表中，找到要编辑的 Gmail 账号
   - 点击账号右侧的"..."菜单
   - 选择"编辑"

3. **启用自定义服务器配置**
   - 在编辑界面中，找到"服务器配置"部分
   - 打开"自定义服务器配置"开关

4. **修改 SMTP 配置**
   - 展开"服务器设置"
   - 找到"发送邮件服务器 (SMTP)"部分
   - 修改以下设置：
     - **服务器地址**：保持 `smtp.gmail.com`（不变）
     - **端口**：从 `587` 改为 `465`
     - **加密方式**：从 `STARTTLS` 改为 `SSL/TLS`

5. **保存配置**
   - 点击右上角的"保存"按钮
   - 配置会立即生效

## 配置说明

### Gmail SMTP 配置对比

| 配置项 | STARTTLS（默认） | SSL 直接连接（推荐） |
|--------|-----------------|---------------------|
| 服务器地址 | smtp.gmail.com | smtp.gmail.com |
| 端口 | 587 | **465** |
| 加密方式 | STARTTLS | **SSL/TLS** |

### 为什么使用 SSL 端口 465？

- ✅ 更稳定：直接 SSL 连接避免了 STARTTLS 握手的问题
- ✅ Gmail 官方推荐：端口 465 是 Gmail 推荐的 SMTP 端口
- ✅ 避免错误：可以解决 LibEtPan STARTTLS 握手失败的问题

## 注意事项

1. **密码设置**
   - 如果您的 Gmail 账号启用了两步验证，需要使用"应用专用密码"
   - 应用专用密码可以在 Google 账号设置中生成

2. **配置保存**
   - 自定义的 SMTP 配置会保存在账号中
   - 下次编辑账号时，如果配置与预设不同，会自动展开"自定义服务器配置"

3. **测试连接**
   - 修改配置后，建议使用"测试连接"功能验证配置是否正确
   - 如果测试失败，检查端口和加密方式是否正确

## 其他邮箱服务

其他邮箱服务的 SMTP 配置也可以使用相同方法编辑：

- **Outlook/Hotmail**：默认端口 587 (STARTTLS)，也可以尝试 465 (SSL)
- **阿里云企业邮箱**：默认端口 465 (SSL)
- **腾讯企业邮箱**：默认端口 465 (SSL)

