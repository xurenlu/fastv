# 重新签名完成说明

## ✅ 已完成的工作

### 1. 证书确认
- **Team ID**: W49B66SUW3
- **使用的证书**: `Apple Development: suhang hu (9K5FH5XTHD)`
- **证书类型**: Apple Development

### 2. 重新签名的文件
- ✅ `build/typecho.app` - 应用主程序
- ✅ `build/typecho.app/Contents/Frameworks/libonnxruntime.1.23.2.dylib` - ONNX Runtime 库
- ✅ `build/typecho.xcarchive/Products/Applications/typecho.app` - 归档中的应用

### 3. 签名验证
所有文件都已使用相同的证书签名，Team ID 一致。

## 🔧 构建脚本更新

已更新构建脚本 (`Copy ONNX Runtime Library` 和 `Sign ONNX Runtime Library`)，确保：
1. 自动查找与 `DEVELOPMENT_TEAM` (W49B66SUW3) 匹配的证书
2. 使用相同的证书签名库文件和应用
3. 确保签名一致性

## 📝 使用方法

### 方法 1：自动构建（推荐）
在 Xcode 中构建项目时，构建脚本会自动：
1. 复制库文件到 Frameworks 目录
2. 使用正确的证书签名库文件
3. 确保应用和库文件签名一致

### 方法 2：手动重新签名
如果需要手动重新签名，运行：

```bash
# 使用指定的 Team ID
./resign_with_team.sh W49B66SUW3

# 或使用默认脚本（会提示选择证书）
./resign_all.sh
```

## ⚠️ 注意事项

1. **证书匹配**：确保 `DEVELOPMENT_TEAM` 设置为 `W49B66SUW3`
2. **签名一致性**：库文件和应用必须使用相同的证书签名
3. **Team ID 验证**：如果更换证书，确保新证书的 Team ID 匹配

## 🧪 验证签名

检查签名状态：

```bash
# 检查应用签名
codesign -dvv build/typecho.app | grep -E "Authority|TeamIdentifier"

# 检查库文件签名
codesign -dvv build/typecho.app/Contents/Frameworks/libonnxruntime.1.23.2.dylib | grep -E "Authority|TeamIdentifier"
```

两者应该显示相同的 `Authority`。

## 🚀 下一步

1. 在 Xcode 中重新构建项目（⌘B）
2. 运行应用测试（⌘R）
3. 如果仍有问题，检查控制台日志中的签名信息

