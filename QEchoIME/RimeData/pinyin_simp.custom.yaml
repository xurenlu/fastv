# 由轻语输入法自动生成，请勿手改；设置请在 Qecho（轻语）App 中调整。
# 引号保留作拼音音节分隔（如 xi'an），故纯拼音仅提供分号次选。
patch:
  key_binder/bindings/+:
    - { when: has_menu, accept: semicolon, send: 2 }
  menu/page_size: 5
  translator/enable_user_dict: true
